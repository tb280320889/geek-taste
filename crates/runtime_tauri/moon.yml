language: 'rust'

tasks:
  build:
    command: 'cargo build -p runtime_tauri'
    toolchain: 'rust'
  test:
    command: 'cargo test -p runtime_tauri'
    toolchain: 'rust'
  check:
    command: 'cargo check -p runtime_tauri'
    toolchain: 'rust'
  lint:
    command: 'cargo clippy -p runtime_tauri -- -D warnings'
    toolchain: 'rust'
  format:
    command: 'cargo fmt'
    toolchain: 'rust'
