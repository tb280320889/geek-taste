language: 'rust'

tasks:
  build:
    command: 'cargo build -p github_adapter'
    toolchain: 'rust'
  test:
    command: 'cargo test -p github_adapter'
    toolchain: 'rust'
  check:
    command: 'cargo check -p github_adapter'
    toolchain: 'rust'
  lint:
    command: 'cargo clippy -p github_adapter -- -D warnings'
    toolchain: 'rust'
  format:
    command: 'cargo fmt'
    toolchain: 'rust'
