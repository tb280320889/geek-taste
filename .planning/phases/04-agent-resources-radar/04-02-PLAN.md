---
phase: 04-agent-resources-radar
plan: 02
type: execute
wave: 2
depends_on:
  - 04-01
files_modified:
  - crates/shared_contracts/src/resource_dto.rs
  - crates/shared_contracts/src/lib.rs
  - crates/application/src/resource.rs
  - crates/application/src/lib.rs
  - crates/runtime_tauri/src/commands/resource.rs
  - crates/runtime_tauri/src/commands/mod.rs
autonomous: true
requirements:
  - RES-02
user_setup: []

must_haves:
  truths:
    - "系统能按 stack_relevance + star_delta + recency 为资源评分排序"
    - "推荐解释使用模板规则生成（语言匹配、框架匹配、增长信号、用户精选）"
    - "Tauri IPC 命令可列出、搜索、精选资源"
    - "application/resource.rs 编排资源发现、评分、解释、精选逻辑"
  artifacts:
    - path: "crates/shared_contracts/src/resource_dto.rs"
      provides: "ResourceCardDto — 前端契约对象"
      exports: ["ResourceCardDto", "ResourceListRequest", "CurateResourceRequest"]
    - path: "crates/application/src/resource.rs"
      provides: "资源用例编排：评分、解释、精选、列表"
      exports: ["list_resources", "search_resources", "curate_resource", "compute_resource_score_with_explanation", "deactivate_resource"]
    - path: "crates/runtime_tauri/src/commands/resource.rs"
      provides: "Tauri IPC 命令：list_resources, search_resources, curate_resource, deactivate_resource"
  key_links:
    - from: "crates/application/src/resource.rs"
      to: "crates/domain/src/resource.rs"
      via: "use domain::resource::{Resource, ResourceScore, RecommendationReason}"
      pattern: "use domain::resource::"
    - from: "crates/application/src/resource.rs"
      to: "crates/persistence_sqlite/src/resource_repository.rs"
      via: "use persistence_sqlite::resource_repository"
      pattern: "persistence_sqlite::resource_repository::"
    - from: "crates/runtime_tauri/src/commands/resource.rs"
      to: "crates/application/src/resource.rs"
      via: "application::resource::"
      pattern: "application::resource::"
    - from: "crates/shared_contracts/src/resource_dto.rs"
      to: "crates/domain/src/resource.rs"
      via: "From<Resource> for ResourceCardDto"
      pattern: "impl From<Resource>"
---

<objective>
Phase 04 Plan 02: 应用层编排与 Tauri IPC 命令

Purpose: 实现资源评分、推荐解释、精选管理的用例编排 + Tauri IPC 薄封装
Output: shared_contracts/resource_dto.rs, application/resource.rs, runtime_tauri/commands/resource.rs
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/04-agent-resources-radar/04-CONTEXT.md
@.planning/phases/04-agent-resources-radar/04-RESEARCH.md

<!-- 参考模式 -->
@crates/application/src/topk.rs
@crates/shared_contracts/src/ranking_dto.rs
@crates/runtime_tauri/src/commands/topk.rs
@crates/runtime_tauri/src/commands/helpers.rs
@crates/domain/src/resource.rs
@crates/persistence_sqlite/src/resource_repository.rs

<interfaces>
<!-- 以下为 Task 1 创建的关键类型，executor 必须了解 -->

From crates/domain/src/resource.rs (created in Plan 01):
```rust
pub enum ResourceKind { McpServer, Skill, AgentFramework, Other(String) }
pub enum CurationLevel { UserCurated, SystemDiscovered }
pub struct Resource {
    pub resource_id: String,
    pub source_repo_id: Option<i64>,
    pub resource_kind: ResourceKind,
    pub title: String,
    pub summary: Option<String>,
    pub source_url: String,
    pub languages: Vec<String>,
    pub framework_tags: Vec<String>,
    pub agent_tags: Vec<String>,
    pub curation_level: CurationLevel,
    pub last_scored_at: Option<String>,
    pub is_active: bool,
}
pub struct ResourceScore { pub total: f64, pub stack_relevance: f64, pub star_delta_norm: f64, pub recency_norm: f64 }
pub fn compute_resource_score(stack_relevance: f64, star_delta_norm: f64, recency_norm: f64) -> ResourceScore
pub enum RecommendationReason {
    LanguageMatch(String), FrameworkMatch(String), GrowthSignal(i64), UserCurated,
}
impl RecommendationReason { pub fn to_template(&self, resource_title: &str) -> String }
```

From crates/persistence_sqlite/src/resource_repository.rs (created in Plan 01):
```rust
pub fn insert_resource(conn: &Connection, resource: &Resource) -> Result<()>
pub fn upsert_resource(conn: &Connection, resource: &Resource) -> Result<()>
pub fn get_resource(conn: &Connection, resource_id: &str) -> Result<Option<Resource>>
pub fn list_resources(conn: &Connection, limit: i64, offset: i64) -> Result<Vec<Resource>>
pub fn search_resources(conn: &Connection, tag_type: Option<&str>, tag_value: Option<&str>,
    resource_kind: Option<ResourceKind>, language: Option<&str>, min_is_active: bool,
    limit: i64, offset: i64) -> Result<Vec<Resource>>
pub fn insert_tags(conn: &Connection, resource_id: &str, tags: &[ResourceTag]) -> Result<()>
pub fn get_tags(conn: &Connection, resource_id: &str) -> Result<Vec<ResourceTag>>
pub fn deactivate_resource(conn: &Connection, resource_id: &str) -> Result<()>
```

From crates/runtime_tauri/src/commands/helpers.rs:
```rust
fn get_db_connection(app: &AppHandle) -> Result<rusqlite::Connection, String>
fn load_token() -> Result<String, String>  // 从 OS keyring 加载
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 创建 resource DTO + 应用层编排</name>
  <files>crates/shared_contracts/src/resource_dto.rs, crates/shared_contracts/src/lib.rs, crates/application/src/resource.rs, crates/application/src/lib.rs</files>
  <read_first>
    - crates/shared_contracts/src/ranking_dto.rs (DTO 模式：From 转换、Serialize/Deserialize)
    - crates/application/src/topk.rs (用例编排模式：generate_id、use persistence_sqlite)
    - crates/domain/src/resource.rs (来自 Plan 01 的领域类型)
    - crates/persistence_sqlite/src/resource_repository.rs (来自 Plan 01 的 CRUD 函数)
    - crates/shared_contracts/src/lib.rs (添加 pub mod resource_dto)
    - crates/application/src/lib.rs (添加 pub mod resource)
  </read_first>
  <action>
    1. 创建 `crates/shared_contracts/src/resource_dto.rs`:

    ```rust
    use serde::{Deserialize, Serialize};

    /// 资源卡片 DTO — 前端契约对象
    #[derive(Debug, Clone, Serialize, Deserialize)]
    pub struct ResourceCardDto {
        pub resource_id: String,
        pub resource_kind: String,
        pub title: String,
        pub source_repo_id: Option<i64>,
        pub source_url: String,
        pub languages: Vec<String>,
        pub framework_tags: Vec<String>,
        pub agent_tags: Vec<String>,
        pub score: f64,
        pub why_recommended: Vec<String>,  // 推荐解释列表
        pub is_curated: bool,              // 用户精选
        pub is_active: bool,
    }

    /// 资源列表请求 DTO
    #[derive(Debug, Clone, Serialize, Deserialize)]
    pub struct ResourceListRequest {
        pub tag_type: Option<String>,
        pub tag_value: Option<String>,
        pub resource_kind: Option<String>,
        pub language: Option<String>,
        pub limit: Option<i64>,
        pub offset: Option<i64>,
    }

    /// 精选操作请求 DTO
    #[derive(Debug, Clone, Serialize, Deserialize)]
    pub struct CurateResourceRequest {
        pub resource_id: String,
        pub action: String,  // "add" | "remove"
    }
    ```

    在 `crates/shared_contracts/src/lib.rs` 添加 `pub mod resource_dto;`

    2. 创建 `crates/application/src/resource.rs`（参考 topk.rs 模式）:

    ```rust
    use anyhow::{Context, Result};
    use chrono::Utc;
    use domain::resource::{
        CurationLevel, RecommendationReason, Resource, ResourceKind, ResourceScore,
        ResourceTag, compute_resource_score,
    };
    use persistence_sqlite::resource_repository;
    use rusqlite::Connection;
    use shared_contracts::resource_dto::ResourceCardDto;

    /// 生成 ID（复用 topk.rs 的 generate_id 模式）
    fn generate_id(prefix: &str) -> String { /* 同 topk.rs */ }

    /// 计算 stack_relevance（用户设置语言 + 订阅推断的加权组合）
    /// 0.7 * settings_match + 0.3 * subscription_match
    /// match = |intersection(user_langs, resource_langs)| / |union(user_langs, resource_langs)|
    fn compute_stack_relevance(
        conn: &Connection,
        resource_languages: &[String],
    ) -> Result<f64> {
        // 1. 获取用户语言兴趣 (从 settings)
        // 2. 获取订阅仓库的语言分布
        // 3. 计算 0.7 * settings_overlap + 0.3 * subscription_overlap
        // 4. 返回 [0.0, 1.0]
    }

    /// 计算 recency_norm: 1.0 - days_since_push / 365, clamped [0,1]
    fn compute_recency_norm(last_pushed_at: Option<&str>) -> f64 {
        // 解析 last_pushed_at, 计算天数差, 返回 1.0 - days/365, clamp [0,1]
    }

    /// 计算 star_delta_norm: 同 MomentumScore 的 star_delta_norm 逻辑
    fn compute_star_delta_norm(conn: &Connection, repo_id: i64) -> Result<f64> {
        // 查询 repo_snapshots 获取上一次 stargazers_count
        // delta = current - prev
        // norm = clamp(delta / 1000.0, 0.0, 1.0)
    }

    /// 为单个资源生成评分 + 推荐解释
    pub fn compute_resource_score_with_explanation(
        conn: &Connection,
        resource: &Resource,
    ) -> Result<(ResourceScore, Vec<String>)> {
        let stack_relevance = compute_stack_relevance(conn, &resource.languages)?;
        let star_delta_norm = resource.source_repo_id
            .map(|id| compute_star_delta_norm(conn, id).unwrap_or(0.0))
            .unwrap_or(0.0);
        let recency_norm = compute_recency_norm(resource.source_repo_id
            .and_then(|_| None)  // 需要从 repositories 获取 pushed_at
            .as_deref());

        let score = compute_resource_score(stack_relevance, star_delta_norm, recency_norm);

        // 生成推荐解释
        let mut reasons = Vec::new();
        if resource.curation_level == CurationLevel::UserCurated {
            reasons.push(RecommendationReason::UserCurated.to_template(&resource.title));
        }
        for lang in &resource.languages {
            if stack_relevance > 0.3 {
                reasons.push(RecommendationReason::LanguageMatch(lang.clone()).to_template(&resource.title));
                break;
            }
        }
        for tag in &resource.framework_tags {
            reasons.push(RecommendationReason::FrameworkMatch(tag.clone()).to_template(&resource.title));
        }
        if star_delta_norm > 0.5 {
            reasons.push(RecommendationReason::GrowthSignal((star_delta_norm * 1000.0) as i64).to_template(&resource.title));
        }
        if reasons.is_empty() {
            reasons.push(format!("发现新资源: {}", resource.title));
        }

        Ok((score, reasons))
    }

    /// 列出资源并计算评分
    pub fn list_resources(
        conn: &Connection,
        limit: i64,
        offset: i64,
    ) -> Result<Vec<ResourceCardDto>> {
        let resources = resource_repository::list_resources(conn, limit, offset)?;
        resources_to_dtos(conn, resources)
    }

    /// 搜索资源并计算评分
    pub fn search_resources(
        conn: &Connection,
        tag_type: Option<&str>,
        tag_value: Option<&str>,
        resource_kind: Option<ResourceKind>,
        language: Option<&str>,
        limit: i64,
        offset: i64,
    ) -> Result<Vec<ResourceCardDto>> {
        let resources = resource_repository::search_resources(
            conn, tag_type, tag_value, resource_kind, language, true, limit, offset,
        )?;
        resources_to_dtos(conn, resources)
    }

    /// 转换 Resource 列表为 ResourceCardDto 列表（含评分和解释）
    fn resources_to_dtos(conn: &Connection, resources: Vec<Resource>) -> Result<Vec<ResourceCardDto>> {
        let mut dtos = Vec::new();
        for resource in resources {
            let (score, why_recommended) = compute_resource_score_with_explanation(conn, &resource)?;
            dtos.push(ResourceCardDto {
                resource_id: resource.resource_id.clone(),
                resource_kind: resource.resource_kind.to_string(),
                title: resource.title.clone(),
                source_repo_id: resource.source_repo_id,
                source_url: resource.source_url.clone(),
                languages: resource.languages.clone(),
                framework_tags: resource.framework_tags.clone(),
                agent_tags: resource.agent_tags.clone(),
                score: score.total,
                why_recommended,
                is_curated: resource.curation_level == CurationLevel::UserCurated,
                is_active: resource.is_active,
            });
        }
        // 按 score 降序排列
        dtos.sort_by(|a, b| b.score.partial_cmp(&a.score).unwrap_or(std::cmp::Ordering::Equal));
        Ok(dtos)
    }

    /// 用户精选资源
    pub fn curate_resource(
        conn: &Connection,
        resource_id: &str,
        action: &str,
    ) -> Result<ResourceCardDto> {
        // action: "add" → 将 curation_level 更新为 UserCurated
        // action: "remove" → 将 curation_level 恢复为 SystemDiscovered
        // 返回更新后的 ResourceCardDto
    }

    /// 停用资源（设为 is_active = false）
    pub fn deactivate_resource(conn: &Connection, resource_id: &str) -> Result<()> {
        resource_repository::deactivate_resource(conn, resource_id)
    }
    ```

    在 `crates/application/src/lib.rs` 添加 `pub mod resource;`

    3. 添加单元测试:
    - compute_stack_relevance 测试 (settings_langs 为空、有交集、无交集)
    - compute_recency_norm 测试 (null → 0, 今天 → 1.0, 180天前 → 0.5)
    - compute_resource_score_with_explanation 测试
    - list_resources 测试 (插入假数据，验证返回 DTO 列表)
  </action>
  <verify>
    <automated>rtk cargo test -p application resource</automated>
  </verify>
  <done>
    - resource_dto.rs 包含 ResourceCardDto, ResourceListRequest, CurateResourceRequest
    - application/resource.rs 包含 compute_resource_score_with_explanation, list_resources, search_resources, curate_resource, deactivate_resource
    - 评分公式: 0.4 * stack_relevance + 0.35 * star_delta_norm + 0.25 * recency_norm
    - 推荐解释使用 RecommendationReason 模板
    - resources_to_dtos 按 score 降序排列
    - 所有测试通过
    - cargo check -p application 通过
  </done>
</task>

<task type="auto">
  <name>Task 2: 创建 Tauri IPC 命令</name>
  <files>crates/runtime_tauri/src/commands/resource.rs, crates/runtime_tauri/src/commands/mod.rs</files>
  <read_first>
    - crates/runtime_tauri/src/commands/topk.rs (Tauri command 模式：AppHandle, get_db_connection)
    - crates/runtime_tauri/src/commands/helpers.rs (get_db_connection, load_token)
    - crates/runtime_tauri/src/commands/mod.rs (添加 mod resource;)
    - crates/application/src/resource.rs (来自 Task 1 的应用层函数)
    - crates/shared_contracts/src/resource_dto.rs (来自 Task 1 的 DTO)
  </read_first>
  <action>
    1. 创建 `crates/runtime_tauri/src/commands/resource.rs`:

    ```rust
    use shared_contracts::resource_dto::{ResourceCardDto, ResourceListRequest, CurateResourceRequest};
    use tauri::AppHandle;
    use super::helpers::get_db_connection;

    #[tauri::command]
    pub async fn list_resources(
        app: AppHandle,
        limit: Option<i64>,
        offset: Option<i64>,
    ) -> Result<Vec<ResourceCardDto>, String> {
        let conn = get_db_connection(&app)?;
        application::resource::list_resources(&conn, limit.unwrap_or(50), offset.unwrap_or(0))
            .map_err(|e| e.to_string())
    }

    #[tauri::command]
    pub async fn search_resources(
        app: AppHandle,
        request: ResourceListRequest,
    ) -> Result<Vec<ResourceCardDto>, String> {
        let conn = get_db_connection(&app)?;
        let kind = request.resource_kind.and_then(|k| domain::resource::ResourceKind::from_str(&k));
        application::resource::search_resources(
            &conn,
            request.tag_type.as_deref(),
            request.tag_value.as_deref(),
            kind,
            request.language.as_deref(),
            request.limit.unwrap_or(50),
            request.offset.unwrap_or(0),
        ).map_err(|e| e.to_string())
    }

    #[tauri::command]
    pub async fn curate_resource(
        app: AppHandle,
        request: CurateResourceRequest,
    ) -> Result<ResourceCardDto, String> {
        let conn = get_db_connection(&app)?;
        application::resource::curate_resource(&conn, &request.resource_id, &request.action)
            .map_err(|e| e.to_string())
    }

    #[tauri::command]
    pub async fn deactivate_resource(
        app: AppHandle,
        resource_id: String,
    ) -> Result<(), String> {
        let conn = get_db_connection(&app)?;
        application::resource::deactivate_resource(&conn, &resource_id)
            .map_err(|e| e.to_string())
    }
    ```

    2. 在 `crates/runtime_tauri/src/commands/mod.rs` 添加:
    ```rust
    pub mod resource;
    ```

    3. 在 `crates/runtime_tauri/src/lib.rs` 注册命令（添加 resource 命令到 invoke_handler）:
    ```rust
    commands::resource::list_resources,
    commands::resource::search_resources,
    commands::resource::curate_resource,
    commands::resource::deactivate_resource,
    ```
  </action>
  <verify>
    <automated>rtk cargo check -p runtime_tauri</automated>
  </verify>
  <done>
    - resource.rs 包含 4 个 Tauri command: list_resources, search_resources, curate_resource, deactivate_resource
    - mod.rs 包含 pub mod resource
    - lib.rs 注册了 4 个 resource 命令
    - cargo check -p runtime_tauri 通过
  </done>
</task>

</tasks>

<verification>
- cargo check -p shared_contracts 通过
- cargo check -p application 通过
- cargo check -p runtime_tauri 通过
- cargo test -p application resource 通过
- ResourceCardDto 字段与 docs/05 ResourceCard 契约一致
- Tauri 命令参数与前端 IPC 调用一致
</verification>

<success_criteria>
- resource_dto.rs 包含 3 个 DTO 类型
- application/resource.rs 包含 6 个函数（评分、解释、列表、搜索、精选、停用）
- runtime_tauri/commands/resource.rs 包含 4 个 Tauri 命令
- 评分公式: 0.4 * stack_relevance + 0.35 * star_delta_norm + 0.25 * recency_norm
- 推荐解释使用模板规则生成
- 所有测试通过
</success_criteria>

<output>
完成后创建 SUMMARY.md: .planning/phases/04-agent-resources-radar/04-02-SUMMARY.md
</output>
