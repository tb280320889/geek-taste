---
phase: 02-topk
plan: 06
type: execute
wave: 5
depends_on: [05]
files_modified:
  - apps/desktop-ui/src/routes/topk/+page.svelte
  - apps/desktop-ui/src/lib/components/RankingList.svelte
  - apps/desktop-ui/src/lib/components/FilterPanel.svelte
  - apps/desktop-ui/src/lib/components/ViewSelector.svelte
  - apps/desktop-ui/src/lib/components/SubscribePopover.svelte
autonomous: true
requirements:
  - TOPK-01
  - TOPK-02
  - TOPK-03
  - TOPK-04
  - TOPK-05
  - TOPK-06
  - TOPK-07
  - TOPK-08

must_haves:
  truths:
    - "TopK 页面显示视图选择器 + 筛选面板 + 排名列表"
    - "用户可通过下拉选择器切换已保存的 RankingView"
    - "排名列表显示 repo 名称、stars、分数、排名变化标识"
    - "Momentum 视图显示综合分 + hover 展开细分维度 Tooltip"
    - "点击订阅按钮弹出预填 Popover，确认后按钮变为「已订阅」"
    - "视图支持 Pin/Unpin 和删除操作"
    - "首次快照不显示排名变化标识"
  artifacts:
    - path: "apps/desktop-ui/src/routes/topk/+page.svelte"
      provides: "TopK 页面布局（视图选择器 + 筛选 + 排名列表）"
    - path: "apps/desktop-ui/src/lib/components/RankingList.svelte"
      provides: "排名列表渲染（Repo 卡片 + 分数 + 排名变化）"
    - path: "apps/desktop-ui/src/lib/components/FilterPanel.svelte"
      provides: "筛选条件面板（language/stars/sort/k 值）"
    - path: "apps/desktop-ui/src/lib/components/ViewSelector.svelte"
      provides: "视图下拉选择器（Pin/Unpin/删除操作）"
    - path: "apps/desktop-ui/src/lib/components/SubscribePopover.svelte"
      provides: "一键订阅弹出面板"
  key_links:
    - from: "+page.svelte"
      to: "stores/topk.ts"
      via: "$effect() 调用 loadViews，selectView"
    - from: "RankingList.svelte"
      to: "SubscribePopover.svelte"
      via: "点击订阅按钮显示 Popover"
    - from: "FilterPanel.svelte"
      to: "stores/topk.ts"
      via: "筛选变更触发 addView 或 refreshCurrentView"
---

<objective>
Phase 2 Plan 06: 前端 TopK UI

Purpose: 重写 TopK 页面，实现完整的排名发现引擎用户界面
Output: 新增 4 个组件 + 重写 topk 页面
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-topk/02-CONTEXT.md
@.planning/phases/02-topk/02-05-SUMMARY.md
@apps/desktop-ui/src/routes/topk/+page.svelte（当前页面 — 仅搜索框）
@apps/desktop-ui/src/lib/stores/topk.ts
@apps/desktop-ui/src/lib/types.ts
@apps/desktop-ui/src/lib/components/RepoInfoModal.svelte（参考 Modal 卡片模式）
@apps/desktop-ui/src/lib/components/LanguagePicker.svelte（参考多选标签）
@apps/desktop-ui/src/lib/components/SettingsGroup.svelte（参考卡片布局）
@apps/desktop-ui/src/lib/stores/auth.ts（authStatus 变量）

<design_context>
D-05: TopK 页面顶部使用下拉选择器展示当前视图名称
D-06: 下拉列表中支持 Pin/Unpin 排序、删除视图操作
D-09: 列表中每项仅显示综合 Momentum 分 + 排序模式标签
D-10: 鼠标悬停综合分时展开 Tooltip 显示三个细分维度
D-11: 与上次 snapshot 对比：排名上升标绿 +↑N，下降标红 -↓N，不变标灰 —
D-12: 首次快照不显示变化标识
D-13: 点击订阅按钮弹出 Popover 而非 Modal
D-14: 默认 STANDARD / 12h digest / HIGH 立即通知
D-15: 订阅成功 toast + 按钮变「已订阅」

CSS 变量系统：var(--radius), var(--border), var(--accent), var(--accent-2), var(--text), var(--text-muted), var(--danger)
SvelteKit 5 Runes：$state(), $derived(), $effect(), $props()
</design_context>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 创建 ViewSelector + FilterPanel 组件</name>
  <read_first>
    - apps/desktop-ui/src/lib/stores/topk.ts（store API）
    - apps/desktop-ui/src/lib/types.ts（RankingViewSpecDto / FiltersDto）
    - apps/desktop-ui/src/lib/components/LanguagePicker.svelte（多选标签模式）
    - apps/desktop-ui/src/routes/topk/+page.svelte（当前页面样式）
  </read_first>
  <files>apps/desktop-ui/src/lib/components/ViewSelector.svelte, apps/desktop-ui/src/lib/components/FilterPanel.svelte</files>
  <action>
    1. 创建 ViewSelector.svelte:
       - Props: views: RankingViewSpecDto[], currentViewId: string | null
       - Events: onSelect(viewId), onPin(viewId), onDelete(viewId)
       - UI: 下拉按钮显示当前视图名，点击展开列表
       - 列表中 pinned 视图排在前面（标 📌）
       - 每项右侧有 Pin 和 Delete 按钮（icon-only）
       - 底部「创建新视图」入口
       - 使用 Svelte 5 Runes：$props(), $state(), $derived()
       - 样式：card 背景、var(--border) 边框、hover 高亮

    2. 创建 FilterPanel.svelte:
       - Props: filters: FiltersDto, rankingMode: string, kValue: number
       - Events: onApply(filters, mode, k)
       - UI: 可折叠面板，包含：
         - Language 多选标签（复用 LanguagePicker 风格）
         - 排序模式下拉（STARS_DESC / UPDATED_DESC / MOMENTUM_24H / MOMENTUM_7D）
         - Min Stars 数字输入
         - K 值滑块或数字输入（1-1000）
         - Exclude Archived / Exclude Forks 开关
         - 「应用筛选」按钮
       - 样式：SettingsGroup 卡片布局风格
  </action>
  <verify>
    <automated>npx tsc --noEmit --project apps/desktop-ui</automated>
  </verify>
  <done>两个组件编译通过，props/events 类型正确</done>
</task>

<task type="auto">
  <name>Task 2: 创建 RankingList + SubscribePopover 组件</name>
  <read_first>
    - apps/desktop-ui/src/lib/types.ts（RankingItemDto / ScoreBreakdownDto）
    - apps/desktop-ui/src/lib/components/RepoInfoModal.svelte（卡片模式）
    - apps/desktop-ui/src/lib/stores/auth.ts（authStatus）
  </read_first>
  <files>apps/desktop-ui/src/lib/components/RankingList.svelte, apps/desktop-ui/src/lib/components/SubscribePopover.svelte</files>
  <action>
    1. 创建 RankingList.svelte:
       - Props: items: RankingItemDto[], loading: boolean, error: string | null
       - Events: onSubscribe(repoId, fullName)
       - UI: 渲染排名列表，每项显示：
         - rank 序号
         - full_name 链接（点击打开 html_url）
         - primary_language 标签
         - stars 数量
         - 综合 score（D-09: 仅 Momentum 模式显示）
         - 排名变化标识（D-11）:
           - rank_change > 0: <span class="rank-up">+↑{rank_change}</span> (绿色)
           - rank_change < 0: <span class="rank-down">-↓{abs}</span> (红色)
           - rank_change === 0: <span class="rank-same">—</span> (灰色)
           - rank_change === null: 不显示（D-12: 首次快照）
         - score_breakdown Tooltip（D-10）:
           - 包裹在 span 上，hover 显示
           - Tooltip 内容："⭐ {star_delta} 🔀 {fork_delta} ⏱ {updated_recency}"
         - subscribe 按钮:
           - is_subscribed === true: 「已订阅」disabled 状态
           - is_subscribed === false: 「订阅」按钮，点击触发 onSubscribe
       - loading 状态：显示 skeleton 占位
       - empty 状态：显示「暂无结果」提示
       - 样式：每项 card 卡片，grid 布局

    2. 创建 SubscribePopover.svelte:
       - Props: repoId: number, fullName: string, visible: boolean
       - Events: onConfirm(settings), onClose()
       - UI: Popover（D-13，非 Modal）:
         - 标题：「订阅 {fullName}」
         - 预填默认值（D-14）:
           - tracking_mode: STANDARD
           - digest_window: 12h
           - notify_high_immediately: true
           - event_types: [RELEASE_PUBLISHED, TAG_PUBLISHED]（排除 DEFAULT_BRANCH_ACTIVITY_DIGEST）
         - 可微调：digest_window 切换（12h/24h）、notify 开关
         - 「确认订阅」按钮
         - 点击外部关闭
       - 样式：紧凑 popover，absolute 定位
  </action>
  <verify>
    <automated>npx tsc --noEmit --project apps/desktop-ui</automated>
  </verify>
  <done>两个组件编译通过，排名变化标识逻辑正确，Popover 定位可用</done>
</task>

<task type="auto">
  <name>Task 3: 重写 TopK 页面</name>
  <read_first>
    - apps/desktop-ui/src/routes/topk/+page.svelte（当前页面 — 需重写）
    - apps/desktop-ui/src/lib/components/ViewSelector.svelte
    - apps/desktop-ui/src/lib/components/FilterPanel.svelte
    - apps/desktop-ui/src/lib/components/RankingList.svelte
    - apps/desktop-ui/src/lib/components/SubscribePopover.svelte
    - apps/desktop-ui/src/lib/stores/topk.ts
    - apps/desktop-ui/src/lib/stores/auth.ts
  </read_first>
  <files>apps/desktop-ui/src/routes/topk/+page.svelte</files>
  <action>
    重写 +page.svelte，替换当前搜索框为完整 TopK 排名引擎 UI：

    ```svelte
    <script lang="ts">
      import ViewSelector from "$lib/components/ViewSelector.svelte";
      import FilterPanel from "$lib/components/FilterPanel.svelte";
      import RankingList from "$lib/components/RankingList.svelte";
      import SubscribePopover from "$lib/components/SubscribePopover.svelte";
      import {
        rankingViews, currentViewId, currentView,
        rankingItems, topkLoading, topkError,
        loadViews, selectView, addView, removeView, pinView,
      } from "$lib/stores/topk.ts";
      import { authStatus } from "$lib/stores/auth";

      let showFilter = $state(false);
      let subscribeTarget = $state<{ repoId: number; fullName: string } | null>(null);

      // 初始化：加载已保存视图
      $effect(() => {
        if ($authStatus === "authenticated") {
          void loadViews();
        }
      });

      // 视图切换
      const handleSelectView = (viewId: string) => {
        void selectView(viewId);
      };

      // 应用筛选 → 创建新视图或刷新
      const handleApplyFilter = (filters, mode, k) => {
        void addView({ name: `自定义 ${new Date().toLocaleString()}`, filters, ranking_mode: mode, k_value: k });
        showFilter = false;
      };

      // 订阅
      const handleSubscribe = (repoId: number, fullName: string) => {
        subscribeTarget = { repoId, fullName };
      };

      // 订阅确认
      const handleSubscribeConfirm = (settings) => {
        // TODO: Phase 3 实现实际订阅
        // 当前仅 toast 提示
        subscribeTarget = null;
      };
    </script>

    <section class="grid gap-3.5">
      <header class="flex items-center justify-between">
        <div>
          <h1 class="m-0 text-2xl font-semibold">TopK 发现</h1>
          <p class="muted">按趋势发现值得关注的开源项目</p>
        </div>
        <button onclick={() => (showFilter = !showFilter)}
          class="cursor-pointer rounded-[var(--radius-sm)] border border-[color:var(--border)] bg-transparent px-3 py-1.5 text-sm">
          {showFilter ? "收起筛选" : "筛选条件"}
        </button>
      </header>

      {#if $authStatus !== "authenticated"}
        <div class="card rounded-[var(--radius)] p-4">请先连接 GitHub 后再使用 TopK。</div>
      {:else}
        <!-- 视图选择器 -->
        <ViewSelector
          views={$rankingViews}
          currentViewId={$currentViewId}
          onSelect={handleSelectView}
          onPin={(id) => void pinView(id)}
          onDelete={(id) => void removeView(id)}
        />

        <!-- 筛选面板（可折叠） -->
        {#if showFilter}
          <FilterPanel
            filters={$currentView?.filters ?? defaultFilters}
            rankingMode={$currentView?.ranking_mode ?? "STARS_DESC"}
            kValue={$currentView?.k_value ?? 50}
            onApply={handleApplyFilter}
          />
        {/if}

        <!-- 错误提示 -->
        {#if $topkError}
          <div class="card rounded-[var(--radius)] p-3 text-[color:var(--danger)]">{$topkError}</div>
        {/if}

        <!-- 排名列表 -->
        <RankingList
          items={$rankingItems}
          loading={$topkLoading}
          error={$topkError}
          onSubscribe={handleSubscribe}
        />

        <!-- 订阅 Popover -->
        {#if subscribeTarget}
          <SubscribePopover
            repoId={subscribeTarget.repoId}
            fullName={subscribeTarget.fullName}
            visible={true}
            onConfirm={handleSubscribeConfirm}
            onClose={() => (subscribeTarget = null)}
          />
        {/if}
      {/if}
    </section>
    ```

    样式遵循现有模式：grid gap-3.5, card rounded, CSS 变量, muted class
  </action>
  <verify>
    <automated>npx tsc --noEmit --project apps/desktop-ui</automated>
  </verify>
  <done>TopK 页面编译通过，布局完整，auth guard 生效</done>
</task>

</tasks>

<verification>
```bash
npx tsc --noEmit --project apps/desktop-ui
cargo check -p desktop-ui-tauri
```
</verification>

<success_criteria>
- [ ] TopK 页面显示视图选择器 + 筛选面板 + 排名列表
- [ ] 视图切换触发 execute_ranking 并更新列表
- [ ] 排名变化标识：绿/红/灰 正确显示
- [ ] Momentum 分数 Tooltip 展开显示细分维度
- [ ] 订阅按钮弹出 Popover 非 Modal
- [ ] 首次快照不显示排名变化标识
- [ ] 未认证状态显示提示卡
- [ ] TypeScript + Rust 编译全部通过
</success_criteria>

<output>
完成后创建 .planning/phases/02-topk/02-06-SUMMARY.md
</output>
