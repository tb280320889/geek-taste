---
phase: 05-polish-release
plan: 05
type: execute
wave: 4
depends_on:
  - "05-04"
files_modified:
  - src-tauri/tauri.conf.json
  - src-tauri/Cargo.toml
  - Cargo.toml
autonomous: false
requirements:
  - HOME-03

must_haves:
  truths:
    - "应用可打包为安装包（Windows .msi + macOS .dmg + Linux .AppImage）"
    - "代码签名配置正确"
    - "自动更新机制可用"
    - "Phase 5 全部成功标准达成"
  artifacts:
    - path: "src-tauri/tauri.conf.json"
      provides: "Tauri 打包配置"
      contains: "bundle"
    - path: "src-tauri/Cargo.toml"
      provides: "Tauri 依赖与 feature flags"
  key_links:
    - from: "src-tauri/tauri.conf.json"
      to: "src-tauri/Cargo.toml"
      via: "bundle 配置与 Cargo features 对齐"
      pattern: "bundle"
---

<objective>
**Phase 05-05: 打包发布 + 自动更新**

配置 Tauri v2 打包、代码签名、自动更新机制，准备发布安装包。

Purpose: 应用可打包为跨平台安装包，支持自动更新，具备发布条件。
Output: Tauri 打包配置 + 签名设置 + updater 端点 + 人工验证 checkpoint
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@src-tauri/tauri.conf.json
@src-tauri/Cargo.toml
@Cargo.toml

<interfaces>
Tauri v2 打包相关配置：
```json
{
  "bundle": {
    "active": true,
    "targets": ["msi", "dmg", "appimage"],
    "icon": ["icons/icon.png"],
    "windows": { "wix": { "template": "..." } },
    "macOS": { "minimumSystemVersion": "10.15" }
  },
  "plugins": {
    "updater": {
      "endpoints": ["..."],
      "pubkey": "..."
    }
  }
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Tauri 打包配置</name>
  <files>
    src-tauri/tauri.conf.json
    src-tauri/Cargo.toml
  </files>
  <read_first>
    src-tauri/tauri.conf.json
    src-tauri/Cargo.toml
    Cargo.toml
  </read_first>
  <action>
    1. 检查 `src-tauri/tauri.conf.json` 的 bundle 配置：
       - 确认 `bundle.active: true`
       - 确认 `bundle.targets` 包含平台对应的安装包格式
       - 确认 icon 配置正确（至少 32x32, 128x128, 256x256 PNG）
    2. 配置代码签名（如需要）：
       - Windows: 配置 Authenticode 签名证书路径
       - macOS: 配置 codesign identity
       - 在环境变量或 CI 中设置签名密钥
    3. 确保 `src-tauri/Cargo.toml` 包含必要 feature flags：
       ```toml
       [features]
       default = ["custom-protocol"]
       custom-protocol = ["tauri/custom-protocol"]
       ```
    4. 验证打包命令: `moon run desktop-ui:build` 或 `cargo tauri build`
  </action>
  <verify>
    <automated>cd "D:\dev\projects\tauri\geek-taste" && grep -c "bundle\|targets\|icon" src-tauri/tauri.conf.json</automated>
  </verify>
  <done>
    - bundle.active 为 true
    - targets 配置了对应平台安装包格式
    - icon 配置正确
    - 打包命令可执行
  </done>
</task>

<task type="auto">
  <name>Task 2: 自动更新 + 发布验证</name>
  <files>
    src-tauri/tauri.conf.json
  </files>
  <read_first>
    src-tauri/tauri.conf.json
  </read_first>
  <action>
    1. 检查 Tauri v2 updater 配置：
       ```json
       {
         "plugins": {
           "updater": {
             "endpoints": [
               "https://your-server.com/updates/{{target}}/{{arch}}/{{current_version}}"
             ],
             "pubkey": "YOUR_PUBLIC_KEY"
           }
         }
       }
       ```
    2. 如未配置，添加 updater 端点占位符（实际部署时替换）
    3. 生成签名密钥对（如需要）：
       - `tauri signer generate -w ~/.tauri/myapp.key`
       - 将公钥添加到 tauri.conf.json
    4. 验证 updater 功能可通过手动触发检查
  </action>
  <verify>
    <automated>cd "D:\dev\projects\tauri\geek-taste" && grep -c "updater" src-tauri/tauri.conf.json</automated>
  </verify>
  <done>
    - updater 端点已配置（或有占位符）
    - 签名密钥对已生成或配置
    - 可手动触发更新检查
  </done>
</task>

<checkpoint:human-verify>
  <what-built>Phase 5 完整交付：离线降级 + 错误处理 + P0-P3 Bug 修复 + 性能优化 + 打包发布</what-built>
  <how-to-verify>
    1. 运行完整 QA 回归测试，确认所有 P0-P3 问题已修复
    2. 测试离线流程：断网 → 打开应用 → 验证缓存 + STALE 标识
    3. 测试安装包：安装 → 首次启动 → 认证 → 全功能走查
    4. 确认 Home 页面在有/无订阅时都正常展示
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</checkpoint:human-verify>

</tasks>

<verification>
Phase 05-05 整体验证:
1. Tauri 打包配置完整
2. updater 配置就绪
3. 人工验证 checkpoint 通过
</verification>

<success_criteria>
- [ ] bundle.active 为 true，targets 正确配置
- [ ] updater 端点和 pubkey 已配置
- [ ] 打包命令可产出安装包
- [ ] 人工验证 checkpoint 通过
</success_criteria>

<output>
After completion, create `.planning/phases/05-polish-release/05-05-SUMMARY.md`
</output>
