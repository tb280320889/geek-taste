---
phase: 05-polish-release
plan: 03
type: execute
wave: 3
depends_on:
  - "05-02"
files_modified:
  - apps/desktop-ui/src/routes/topk/+page.svelte
  - apps/desktop-ui/src/routes/+page.svelte
  - apps/desktop-ui/src/lib/ipc/tauri.ts
  - src-tauri/tauri.conf.json
autonomous: true
requirements:
  - HOME-01
  - HOME-02

must_haves:
  truths:
    - "点击'在 GitHub 打开'按钮能在系统浏览器中打开对应链接"
    - "Home 页面无订阅时仍展示示例信号或引导内容"
  artifacts:
    - path: "apps/desktop-ui/src/routes/topk/+page.svelte"
      provides: "TopK 资源卡片 GitHub 外部链接"
      contains: "shell.openExternal"
    - path: "apps/desktop-ui/src/routes/+page.svelte"
      provides: "Home 页面空状态引导"
      contains: "empty-state"
  key_links:
    - from: "apps/desktop-ui/src/routes/topk/+page.svelte"
      to: "apps/desktop-ui/src/lib/ipc/tauri.ts"
      via: "外部链接调用 shell.openExternal"
      pattern: "openExternal"
    - from: "apps/desktop-ui/src/routes/+page.svelte"
      to: "apps/desktop-ui/src/lib/stores/signals.ts"
      via: "Home 页面订阅信号 store"
      pattern: "signals"
---

<objective>
**Phase 05-03: P1 功能缺陷修复**

修复 QA 发现的 2 个 P1 问题：GitHub 外部链接无响应、Home 页面无订阅时不可体验。

Purpose: 确保外部链接正常工作，新用户无订阅时也能体验 Home 页面。
Output: GitHub 链接可正常打开 + Home 页面空状态引导
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/QA-FINDINGS.md
@.planning/NEXT-ACTIONS.md
@apps/desktop-ui/src/routes/topk/+page.svelte
@apps/desktop-ui/src/routes/+page.svelte
@apps/desktop-ui/src/lib/ipc/tauri.ts
@apps/desktop-ui/src/lib/stores/signals.ts
@apps/desktop-ui/src/lib/stores/auth.ts
@src-tauri/tauri.conf.json

<interfaces>
<!-- Key types and patterns the executor needs -->

From ipc/tauri.ts:
```typescript
export const openExternal = async (url: string): Promise<void> =>
  invoke("open_external", { url });
```

From tauri.conf.json — check shell permission:
```json
{
  "plugins": {
    "shell": {
      "open": true
    }
  }
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: GitHub 外部链接修复 (QA #4)</name>
  <files>
    apps/desktop-ui/src/routes/topk/+page.svelte
    apps/desktop-ui/src/lib/ipc/tauri.ts
    src-tauri/tauri.conf.json
  </files>
  <read_first>
    apps/desktop-ui/src/routes/topk/+page.svelte
    apps/desktop-ui/src/lib/ipc/tauri.ts
    src-tauri/tauri.conf.json
    src-tauri/capabilities/default.json
  </read_first>
  <action>
    1. 检查 TopK 页面中"在 GitHub 打开"按钮的点击事件绑定
    2. 检查 Tauri v2 shell 权限配置：
       - 确认 `src-tauri/capabilities/default.json` 或 `tauri.conf.json` 中 shell.open 权限已启用
       - 确认 Cargo.toml 中 `tauri-plugin-shell` 依赖已添加
    3. 修复外部链接打开逻辑：
       - 如果使用 `window.open()`，改为 Tauri shell plugin: `import { open } from '@tauri-apps/plugin-shell'; await open(url);`
       - 如果已有 shell 调用但 CSP 阻止，更新 CSP 策略允许外部链接
    4. 在 IPC wrapper 中添加 `openExternal` 函数（如不存在）
    5. 确保按钮事件正确调用并处理错误
  </action>
  <verify>
    <automated>cd "D:\dev\projects\tauri\geek-taste" && grep -c "shell\|openExternal\|open(" apps/desktop-ui/src/routes/topk/+page.svelte</automated>
  </verify>
  <done>
    - "在 GitHub 打开"按钮调用 Tauri shell plugin 打开链接
    - Tauri shell 权限配置正确
    - 点击按钮后系统浏览器打开 GitHub URL
  </done>
</task>

<task type="auto">
  <name>Task 2: Home 页面无订阅引导 (QA #5)</name>
  <files>
    apps/desktop-ui/src/routes/+page.svelte
  </files>
  <read_first>
    apps/desktop-ui/src/routes/+page.svelte
    apps/desktop-ui/src/lib/stores/signals.ts
    apps/desktop-ui/src/lib/stores/subscriptions.ts
    apps/desktop-ui/src/lib/stores/auth.ts
  </read_first>
  <action>
    1. 检查 Home 页面是否有空状态处理
    2. 添加空订阅时的引导状态：
       ```svelte
       {#if $subscriptions.length === 0}
         <div class="empty-state">
           <h2>欢迎来到 Geek Taste</h2>
           <p>开始发现你关注的技术项目</p>
           <a href="/topk">去 TopK 发现趋势项目</a>
           <a href="/subscriptions">搜索并订阅仓库</a>
         </div>
       {:else}
         <!-- 现有信号摘要内容 -->
       {/if}
       ```
    3. 设计引导卡片样式：居中布局、图标、描述文字、行动按钮
    4. 可选：展示示例信号数据（静态 mock），让新用户了解信号聚合功能
    5. 确保有订阅时仍正常展示信号摘要
  </action>
  <verify>
    <automated>cd "D:\dev\projects\tauri\geek-taste" && grep -c "empty\|引导\|welcome\|onboarding" apps/desktop-ui/src/routes/+page.svelte</automated>
  </verify>
  <done>
    - 无订阅时 Home 页面展示引导内容（而非空白）
    - 引导内容包含导航链接（TopK / Subscriptions）
    - 有订阅时正常展示信号摘要
  </done>
</task>

</tasks>

<verification>
Phase 05-03 整体验证:
1. 点击 GitHub 外部链接按钮可在系统浏览器打开 URL
2. 无订阅时 Home 页面展示引导内容
3. 有订阅时 Home 页面正常展示信号摘要
</verification>

<success_criteria>
- [ ] "在 GitHub 打开"按钮调用 Tauri shell plugin 打开链接
- [ ] Home 页面无订阅时展示引导卡片
- [ ] Home 页面有订阅时正常展示信号摘要
</success_criteria>

<output>
After completion, create `.planning/phases/05-polish-release/05-03-SUMMARY.md`
</output>
