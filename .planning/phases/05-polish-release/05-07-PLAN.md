---
phase: 05-polish-release
plan: 07
type: execute
wave: 2
depends_on:
  - 05-06
files_modified:
  - apps/desktop-ui/src/routes/+layout.svelte
  - apps/desktop-ui/src/app.css
autonomous: true
gap_closure: true
requirements: []

must_haves:
  truths:
    - "离线时应用顶部显示网络状态提示"
    - "checkNetworkStatus() 在应用启动时被调用"
  artifacts:
    - path: "apps/desktop-ui/src/routes/+layout.svelte"
      provides: "离线 banner 渲染 — 导入 isOnline store，在 {#if !$isOnline} 中显示提示"
      min_lines: 50
    - path: "apps/desktop-ui/src/app.css"
      provides: ".offline-banner 样式"
      contains: ".offline-banner"
  key_links:
    - from: "+layout.svelte"
      to: "network.ts"
      via: "import { isOnline, checkNetworkStatus }"
      pattern: "import.*isOnline.*from.*network"
    - from: "+layout.svelte onMount"
      to: "checkNetworkStatus()"
      via: "函数调用"
      pattern: "checkNetworkStatus"
---

# Gap Plan 07: Offline Banner UI

**Purpose:** 将已实现的 isOnline store 接入 +layout.svelte 渲染离线 banner。Verification 发现 isOnline store 存在但 layout 未导入、checkNetworkStatus 从未被调用、app.css 无 offline-banner 样式。

**Output:** 应用启动时检测网络状态；离线时在页面顶部显示琥珀色 banner 提示。

## Context

@apps/desktop-ui/src/routes/+layout.svelte
@apps/desktop-ui/src/lib/stores/network.ts
@apps/desktop-ui/src/app.css

### Current State

`+layout.svelte` 44 行：
- 导入: authStatus, initAuth (无 isOnline / checkNetworkStatus)
- onMount: 仅调用 initAuth()
- 渲染: onboarding / unauthenticated / children 三个分支

`network.ts` 48 行：
- `isOnline` — writable(true)
- `checkNetworkStatus()` — async, 调用 getSyncStatus IPC, 设置 isOnline/lastSyncedAt
- `handleIpcError()` — 网络错误时设 isOnline(false)

<tasks>

<task type="auto">
  <name>Task 1: 接入离线 banner 并添加样式</name>
  <files>apps/desktop-ui/src/routes/+layout.svelte, apps/desktop-ui/src/app.css</files>
  <read_first>
    - `apps/desktop-ui/src/routes/+layout.svelte` — 当前无 isOnline 导入
    - `apps/desktop-ui/src/lib/stores/network.ts` — isOnline + checkNetworkStatus 签名
    - `apps/desktop-ui/src/app.css` — 现有 CSS 变量和基础样式
  </read_first>
  <action>
    1. 编辑 `apps/desktop-ui/src/routes/+layout.svelte`:

    在 script 部分添加导入（在 auth import 之后）：
    ```typescript
    import { isOnline, checkNetworkStatus } from "$lib/stores/network";
    ```

    修改 onMount 添加 checkNetworkStatus 调用：
    ```typescript
    onMount(() => {
      void initAuth();
      void checkNetworkStatus();
    });
    ```

    在 `<main class="flex-1 overflow-auto p-3 lg:p-5">` 内、`{#if $authStatus}` 之前添加离线 banner：
    ```svelte
    {#if !$isOnline}
      <div class="offline-banner" role="alert">
        ⚠ 网络不可用 — 使用缓存数据，部分功能受限
      </div>
    {/if}
    ```

    2. 编辑 `apps/desktop-ui/src/app.css`，在 `.muted` 规则之后追加：
    ```css
    .offline-banner {
      background: rgba(255, 193, 7, 0.12);
      border: 1px solid rgba(255, 193, 7, 0.35);
      border-radius: var(--radius-sm);
      color: #ffc107;
      padding: 8px 14px;
      margin-bottom: 12px;
      font-size: 0.85rem;
      text-align: center;
    }
    ```
  </action>
  <verify>
    <automated>npx svelte-check --fail-on-warnings 2>&1 | tail -5</automated>
  </verify>
  <done>
    - +layout.svelte 导入 isOnline + checkNetworkStatus
    - onMount 调用 checkNetworkStatus()
    - {#if !$isOnline} 渲染 .offline-banner
    - app.css 包含 .offline-banner 样式规则
    - svelte-check 0 errors
  </done>
</task>

</tasks>

## Verification

```bash
# 导入检查
grep "isOnline" apps/desktop-ui/src/routes/+layout.svelte
grep "checkNetworkStatus" apps/desktop-ui/src/routes/+layout.svelte

# 渲染检查
grep "offline-banner" apps/desktop-ui/src/routes/+layout.svelte

# 样式检查
grep ".offline-banner" apps/desktop-ui/src/app.css

# 编译检查
npx svelte-check 2>&1 | grep -c "Error"
```

## Success Criteria

- [x] isOnline 和 checkNetworkStatus 从 network.ts 导入
- [x] checkNetworkStatus 在 onMount 中被调用
- [x] 离线时 banner 可见
- [x] svelte-check 0 errors

## Output

After completion, create SUMMARY: `.planning/phases/05-polish-release/05-07-SUMMARY.md`
