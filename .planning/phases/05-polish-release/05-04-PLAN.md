---
phase: 05-polish-release
plan: 04
type: execute
wave: 3
depends_on:
  - "05-02"
  - "05-03"
files_modified:
  - apps/desktop-ui/src/routes/settings/+page.svelte
  - apps/desktop-ui/src/lib/stores/auth.ts
  - apps/desktop-ui/static/favicon.ico
  - apps/desktop-ui/vite.config.ts
  - apps/desktop-ui/src/routes/+layout.svelte
  - apps/desktop-ui/src/routes/+page.svelte
  - apps/desktop-ui/src/routes/topk/+page.svelte
autonomous: true
requirements: []

must_haves:
  truths:
    - "Settings 页面 Toast 不导致布局抖动"
    - "用户可从 Settings 或 Sidebar 注销"
    - "应用有 favicon，控制台不报 404"
    - "列表渲染性能达标，无明显卡顿"
  artifacts:
    - path: "apps/desktop-ui/src/routes/settings/+page.svelte"
      provides: "Toast 布局修复 + 注销按钮"
      contains: "position: fixed"
    - path: "apps/desktop-ui/src/lib/stores/auth.ts"
      provides: "logout 方法"
      contains: "logout"
    - path: "apps/desktop-ui/static/favicon.ico"
      provides: "favicon 文件"
  key_links:
    - from: "apps/desktop-ui/src/routes/settings/+page.svelte"
      to: "apps/desktop-ui/src/lib/stores/auth.ts"
      via: "logout 按钮调用 auth store"
      pattern: "logout"
---

<objective>
**Phase 05-04: P2/P3 + 性能优化 + 打磨**

修复 QA P2 问题（Toast 抖动、缺少注销）、P3 问题（favicon 404），并优化性能和体验。

Purpose: 消除体验瑕疵，添加注销功能，修复 favicon，优化列表渲染性能。
Output: Toast fixed 定位 + logout 功能 + favicon + 性能优化
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/QA-FINDINGS.md
@.planning/NEXT-ACTIONS.md
@apps/desktop-ui/src/routes/settings/+page.svelte
@apps/desktop-ui/src/lib/stores/auth.ts
@apps/desktop-ui/src/routes/+layout.svelte
@apps/desktop-ui/src/routes/+page.svelte
@apps/desktop-ui/src/routes/topk/+page.svelte
@apps/desktop-ui/vite.config.ts

<interfaces>
From stores/auth.ts:
```typescript
export const isAuthenticated: Writable<boolean> = writable(false);
export const user: Writable<GitHubUser | null> = writable(null);
export async function logout(): Promise<void> { ... }
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Toast 布局修复 + 注销功能 (QA #6 + #7)</name>
  <files>
    apps/desktop-ui/src/routes/settings/+page.svelte
    apps/desktop-ui/src/lib/stores/auth.ts
  </files>
  <read_first>
    apps/desktop-ui/src/routes/settings/+page.svelte
    apps/desktop-ui/src/lib/stores/auth.ts
    apps/desktop-ui/src/routes/+layout.svelte
  </read_first>
  <action>
    **Toast 布局修复 (#6)**:
    1. 找到 Settings 页面中 Toast 组件的位置
    2. 将 Toast 容器改为 `position: fixed`，脱离文档流：
       ```css
       .toast {
         position: fixed;
         bottom: 1rem;
         right: 1rem;
         z-index: 1000;
         /* 动画样式 */
         animation: slideIn 0.3s ease;
       }
       ```
    3. 确保 Toast 出现/消失不影响其他组件位置

    **注销功能 (#7)**:
    1. 在 Settings 页面底部添加注销按钮：
       ```svelte
       <button on:click={handleLogout}>注销</button>
       ```
    2. `handleLogout` 调用 auth store 的 `logout()` 方法
    3. logout 后跳转到 onboarding 页面
    4. 确认 auth store 有 `logout` 方法（清除 token + 重置状态）
  </action>
  <verify>
    <automated>cd "D:\dev\projects\tauri\geek-taste" && grep -c "position.*fixed\|logout\|Logout\|注销" apps/desktop-ui/src/routes/settings/+page.svelte</automated>
  </verify>
  <done>
    - Toast 使用 position: fixed，不影响布局
    - Settings 页面有注销按钮
    - 注销后跳转到 onboarding 页面
  </done>
</task>

<task type="auto">
  <name>Task 2: favicon 修复 (QA #8)</name>
  <files>
    apps/desktop-ui/static/favicon.ico
    apps/desktop-ui/vite.config.ts
    apps/desktop-ui/src/app.html
  </files>
  <read_first>
    apps/desktop-ui/src/app.html
    apps/desktop-ui/vite.config.ts
  </read_first>
  <action>
    1. 检查 `apps/desktop-ui/static/` 目录是否有 favicon.ico
    2. 如果不存在，从 `src-tauri/icons/` 复制一个 icon 或生成默认 favicon
    3. 检查 `apps/desktop-ui/src/app.html` 的 `<head>` 中是否有 favicon link：
       ```html
       <link rel="icon" href="%sveltekit.assets%/favicon.ico" />
       ```
    4. 如果使用 Vite 配置，在 `vite.config.ts` 中添加 favicon 配置
    5. 确保开发和生产环境都能正确加载 favicon
  </action>
  <verify>
    <automated>cd "D:\dev\projects\tauri\geek-taste" && ls apps/desktop-ui/static/favicon.ico 2>/dev/null && echo "exists" || echo "missing"</automated>
  </verify>
  <done>
    - favicon.ico 文件存在于 static 目录
    - app.html 引用正确
    - 控制台不再报 favicon 404
  </done>
</task>

<task type="auto">
  <name>Task 3: 性能优化 + 打磨体验</name>
  <files>
    apps/desktop-ui/src/routes/+page.svelte
    apps/desktop-ui/src/routes/topk/+page.svelte
    apps/desktop-ui/src/routes/subscriptions/+page.svelte
  </files>
  <read_first>
    apps/desktop-ui/src/routes/+page.svelte
    apps/desktop-ui/src/routes/topk/+page.svelte
    apps/desktop-ui/src/routes/subscriptions/+page.svelte
  </read_first>
  <action>
    1. 列表渲染优化：
       - 检查是否有不必要的 `$:` 响应式声明导致重复计算
       - 对长列表考虑使用 `{#each items as item (item.id)}` keyed each blocks
       - 减少不必要的 store subscribe/unsubscribe
    2. CSS 动画/过渡：
       - 为页面切换添加淡入淡出过渡
       - 为卡片添加 hover 微交互
       - 为列表项添加入场动画
    3. 启动速度：
       - 检查 layout.svelte onMount 中是否有阻塞操作
       - 确保初始数据加载不阻塞首屏渲染
    4. 清理 console warnings 和无用代码
  </action>
  <verify>
    <automated>cd "D:\dev\projects\tauri\geek-taste" && npx svelte-check --threshold error 2>&1 | tail -5</automated>
  </verify>
  <done>
    - 列表渲染使用 keyed each blocks
    - 添加了必要的 CSS 过渡动画
    - 启动速度合理
    - svelte-check 无 error
  </done>
</task>

</tasks>

<verification>
Phase 05-04 整体验证:
1. Toast 不影响布局位移
2. 注销功能正常工作
3. favicon 不报 404
4. 列表滚动流畅
5. svelte-check 通过
</verification>

<success_criteria>
- [ ] Toast 使用 position: fixed，不影响布局
- [ ] Settings 页面有注销按钮，注销后跳转 onboarding
- [ ] favicon.ico 存在且正确引用
- [ ] 列表渲染使用 keyed each blocks
- [ ] svelte-check 无 error
</success_criteria>

<output>
After completion, create `.planning/phases/05-polish-release/05-04-SUMMARY.md`
</output>
