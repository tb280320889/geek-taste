---
phase: 05-polish-release
plan: 02
type: execute
wave: 2
depends_on:
  - "05-01"
files_modified:
  - apps/desktop-ui/src/routes/subscriptions/+page.svelte
  - apps/desktop-ui/src/routes/topk/+page.svelte
  - apps/desktop-ui/src/lib/stores/topk.ts
  - apps/desktop-ui/src/routes/resources/+page.svelte
  - apps/desktop-ui/src/lib/stores/resources.ts
autonomous: true
requirements: []

must_haves:
  truths:
    - "Subscriptions 页面有搜索输入框可搜索仓库"
    - "TopK 打开后即展示预设的 Trending 排名视图"
    - "Resources 页面有资源卡片列表展示"
  artifacts:
    - path: "apps/desktop-ui/src/routes/subscriptions/+page.svelte"
      provides: "Subscriptions 页面搜索功能"
      contains: "search"
    - path: "apps/desktop-ui/src/routes/topk/+page.svelte"
      provides: "TopK 默认视图加载"
      contains: "defaultView"
    - path: "apps/desktop-ui/src/routes/resources/+page.svelte"
      provides: "Resources 资源卡片渲染"
      contains: "ResourceCard"
  key_links:
    - from: "apps/desktop-ui/src/routes/subscriptions/+page.svelte"
      to: "apps/desktop-ui/src/lib/stores/subscriptions.ts"
      via: "搜索输入绑定 store filter"
      pattern: "search"
    - from: "apps/desktop-ui/src/routes/topk/+page.svelte"
      to: "apps/desktop-ui/src/lib/stores/topk.ts"
      via: "onMount 初始化默认视图"
      pattern: "onMount"
---

<objective>
**Phase 05-02: P0 阻塞 Bug 修复**

修复 QA 发现的 3 个 P0 阻塞问题：订阅搜索框缺失、TopK 默认视图缺失、资源卡片未显示。

Purpose: 确保核心页面功能正常可用，用户打开应用即可使用订阅搜索、TopK 排名和资源列表。
Output: 3 个页面修复完成，搜索/视图/卡片正常渲染
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/QA-FINDINGS.md
@.planning/NEXT-ACTIONS.md
@apps/desktop-ui/src/routes/subscriptions/+page.svelte
@apps/desktop-ui/src/routes/topk/+page.svelte
@apps/desktop-ui/src/routes/resources/+page.svelte
@apps/desktop-ui/src/lib/stores/topk.ts
@apps/desktop-ui/src/lib/stores/resources.ts
@apps/desktop-ui/src/lib/stores/subscriptions.ts
@apps/desktop-ui/src/lib/types.ts

<interfaces>
<!-- Key stores and components the executor needs -->

From stores/topk.ts:
```typescript
export const topkError: Writable<string | null> = writable(null);
export const topkLoading: Writable<boolean> = writable(false);
export async function selectView(viewId: string): Promise<void> { ... }
```

From stores/subscriptions.ts:
```typescript
export const subscriptions: Writable<SubscriptionDto[]> = writable([]);
export async function searchRepos(query: string): Promise<void> { ... }
```

From stores/resources.ts:
```typescript
export const resources: Writable<ResourceDto[]> = writable([]);
export const resourcesLoading: Writable<boolean> = writable(false);
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 订阅搜索框修复 (QA #1)</name>
  <files>
    apps/desktop-ui/src/routes/subscriptions/+page.svelte
  </files>
  <read_first>
    apps/desktop-ui/src/routes/subscriptions/+page.svelte
    apps/desktop-ui/src/lib/stores/subscriptions.ts
    apps/desktop-ui/src/lib/types.ts
  </read_first>
  <action>
    1. 检查 Subscriptions 页面是否导入了搜索组件或搜索输入框
    2. 如果搜索输入框存在但被条件渲染隐藏（如 `{#if false}`），修正条件
    3. 如果搜索组件未导入，添加搜索输入框到页面顶部：
       ```svelte
       <input
         type="text"
         placeholder="搜索仓库..."
         bind:value={searchQuery}
         on:input={handleSearch}
       />
       ```
    4. 绑定 `searchQuery` 变量和 `handleSearch` 调用 subscriptions store 的搜索方法
    5. 确保搜索结果列表正确渲染
  </action>
  <verify>
    <automated>cd "D:\dev\projects\tauri\geek-taste" && grep -c "search\|Search\|搜索" apps/desktop-ui/src/routes/subscriptions/+page.svelte</automated>
  </verify>
  <done>
    - Subscriptions 页面有可见的搜索输入框
    - 输入关键词可触发仓库搜索
    - 搜索结果正确渲染
  </done>
</task>

<task type="auto">
  <name>Task 2: TopK 默认视图修复 (QA #2)</name>
  <files>
    apps/desktop-ui/src/routes/topk/+page.svelte
    apps/desktop-ui/src/lib/stores/topk.ts
  </files>
  <read_first>
    apps/desktop-ui/src/routes/topk/+page.svelte
    apps/desktop-ui/src/lib/stores/topk.ts
    apps/desktop-ui/src/lib/types.ts
  </read_first>
  <action>
    1. 检查 TopK 页面 onMount 逻辑：是否有默认视图加载
    2. 如果 `topkViews` store 为空，创建默认视图（Trending, Most Starred 等）
    3. 在 `topk.ts` store 中添加 `ensureDefaultViews()` 函数：
       - 检查是否有已保存的 views
       - 如果为空，创建 2-3 个预设视图（Trending: stars_desc, Most Starred: stars > 1000, Recently Updated: updated_desc）
       - 自动选中第一个视图并加载数据
    4. 在 `+page.svelte` 的 `onMount` 中调用 `ensureDefaultViews()`
    5. 确保视图列表和排名数据正确渲染
  </action>
  <verify>
    <automated>cd "D:\dev\projects\tauri\geek-taste" && grep -c "ensureDefault\|defaultView\|onMount" apps/desktop-ui/src/routes/topk/+page.svelte</automated>
  </verify>
  <done>
    - 首次打开 TopK 即显示预设的排名视图
    - 视图列表包含 Trending / Most Starred 等预设
    - 排名数据正确加载
  </done>
</task>

<task type="auto">
  <name>Task 3: 资源卡片显示修复 (QA #3)</name>
  <files>
    apps/desktop-ui/src/routes/resources/+page.svelte
    apps/desktop-ui/src/lib/stores/resources.ts
  </files>
  <read_first>
    apps/desktop-ui/src/routes/resources/+page.svelte
    apps/desktop-ui/src/lib/stores/resources.ts
    apps/desktop-ui/src/lib/types.ts
  </read_first>
  <action>
    1. 检查 Resources 页面的资源数据加载逻辑
    2. 确保 onMount 时调用资源获取命令（如 `getResources` IPC）
    3. 检查资源卡片组件是否存在且正确导入
    4. 如果数据源未接入：
       - 在 `resources.ts` store 中添加 `fetchResources()` 函数
       - 调用 Tauri IPC 命令获取资源列表
       - 更新 `resources` store
    5. 修复渲染条件：确保 `resources.length > 0` 时正确展示卡片
    6. 如果无数据，显示空状态提示（"暂无资源数据"）
  </action>
  <verify>
    <automated>cd "D:\dev\projects\tauri\geek-taste" && grep -c "onMount\|fetch\|load\|get" apps/desktop-ui/src/routes/resources/+page.svelte</automated>
  </verify>
  <done>
    - Resources 页面有资源卡片列表展示
    - 数据正确加载并渲染
    - 无数据时显示空状态提示
  </done>
</task>

</tasks>

<verification>
Phase 05-02 整体验证:
1. Subscriptions 页面有搜索输入框，可搜索仓库
2. TopK 首次打开即展示预设排名视图
3. Resources 页面有资源卡片列表
4. 三个页面无运行时错误
</verification>

<success_criteria>
- [ ] Subscriptions 页面有搜索输入框，可搜索并展示结果
- [ ] TopK 首次打开即展示预设的 Trending 排名视图
- [ ] Resources 页面有资源卡片列表展示
- [ ] 三个页面无 console error
</success_criteria>

<output>
After completion, create `.planning/phases/05-polish-release/05-02-SUMMARY.md`
</output>
