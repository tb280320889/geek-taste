---
phase: 05-polish-release
plan: 06
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/runtime_tauri/src/commands/mod.rs
  - apps/desktop-ui/src-tauri/src/lib.rs
autonomous: true
gap_closure: true
requirements: []

must_haves:
  truths:
    - "所有 TopK/订阅/信号/资源/sync_status IPC 命令在运行时可调用"
    - "cargo check -p runtime_tauri 编译通过"
  artifacts:
    - path: "crates/runtime_tauri/src/commands/mod.rs"
      provides: "模块声明 — 将 topk/signal/subscription/resource/sync_status 纳入编译"
      min_lines: 15
    - path: "apps/desktop-ui/src-tauri/src/lib.rs"
      provides: "invoke_handler 注册 — 28 个前端 IPC 调用全部匹配后端命令"
      min_lines: 35
  key_links:
    - from: "commands/mod.rs"
      to: "topk.rs / signal.rs / subscription.rs / resource.rs / sync_status.rs"
      via: "pub mod 声明"
      pattern: "pub mod (topk|signal|subscription|resource|sync_status)"
    - from: "lib.rs handler"
      to: "各模块的 #[tauri::command] 函数"
      via: "generate_handler! 注册"
      pattern: "generate_handler!\["
---

# Gap Plan 06: IPC Command Registration

**Purpose:** 修复 Phases 02-05 遗留的系统性问题 — 所有 topk/signal/subscription/resource/sync_status 模块文件存在于 commands/ 目录但未在 mod.rs 声明、未在 lib.rs handler 注册。前端 ~20 个 invoke() 调用运行时将返回 "command not found"。

**Output:** 28 个 IPC 命令全部注册，前端 invoke() 可正常调用后端。

## Context

@crates/runtime_tauri/src/commands/mod.rs
@crates/runtime_tauri/src/commands/topk.rs
@crates/runtime_tauri/src/commands/signal.rs
@crates/runtime_tauri/src/commands/subscription.rs
@crates/runtime_tauri/src/commands/resource.rs
@crates/runtime_tauri/src/commands/sync_status.rs
@apps/desktop-ui/src-tauri/src/lib.rs
@apps/desktop-ui/src/lib/ipc/tauri.ts

### Command Inventory (from tauri.ts)

**Already registered (8):**
- validate_github_token, store_github_token, load_github_token, remove_github_token
- get_current_user, fetch_repo_info, get_settings, update_settings

**Need registration (20):**
- topk.rs: list_ranking_views, create_ranking_view, delete_ranking_view, toggle_pin_ranking_view, execute_ranking
- signal.rs: list_signals, list_home_signals, ack_signal, mark_signal_seen, get_unread_counts
- subscription.rs: subscribe, unsubscribe, pause_subscription, list_subscriptions, sync_subscriptions
- resource.rs: list_resources, search_resources, curate_resource, deactivate_resource
- sync_status.rs: get_sync_status

<tasks>

<task type="auto">
  <name>Task 1: 声明模块并注册所有 IPC 命令</name>
  <files>crates/runtime_tauri/src/commands/mod.rs, apps/desktop-ui/src-tauri/src/lib.rs</files>
  <read_first>
    - `crates/runtime_tauri/src/commands/mod.rs` — 当前仅声明 auth + settings
    - `apps/desktop-ui/src-tauri/src/lib.rs` — 当前 handler 仅注册 8 个命令
    - `apps/desktop-ui/src/lib/ipc/tauri.ts` — 前端 28 个 invoke 调用（注册清单的权威来源）
  </read_first>
  <action>
    1. 编辑 `crates/runtime_tauri/src/commands/mod.rs`，将内容替换为：
    ```rust
    pub mod auth;
    pub mod settings;
    pub mod topk;
    pub mod signal;
    pub mod subscription;
    pub mod resource;
    pub mod sync_status;

    pub use auth::*;
    pub use settings::*;
    pub use topk::*;
    pub use signal::*;
    pub use subscription::*;
    pub use resource::*;
    pub use sync_status::*;
    ```

    2. 编辑 `apps/desktop-ui/src-tauri/src/lib.rs`，将 `invoke_handler` 替换为注册全部 28 个命令：
    ```rust
    .invoke_handler(tauri::generate_handler![
        // auth + settings (existing)
        runtime_tauri::commands::validate_github_token,
        runtime_tauri::commands::store_github_token,
        runtime_tauri::commands::load_github_token,
        runtime_tauri::commands::remove_github_token,
        runtime_tauri::commands::get_current_user,
        runtime_tauri::commands::fetch_repo_info,
        runtime_tauri::commands::get_settings,
        runtime_tauri::commands::update_settings,
        // topk (Phase 2)
        runtime_tauri::commands::list_ranking_views,
        runtime_tauri::commands::create_ranking_view,
        runtime_tauri::commands::delete_ranking_view,
        runtime_tauri::commands::toggle_pin_ranking_view,
        runtime_tauri::commands::execute_ranking,
        // signal (Phase 3)
        runtime_tauri::commands::list_signals,
        runtime_tauri::commands::list_home_signals,
        runtime_tauri::commands::ack_signal,
        runtime_tauri::commands::mark_signal_seen,
        runtime_tauri::commands::get_unread_counts,
        // subscription (Phase 3)
        runtime_tauri::commands::subscribe,
        runtime_tauri::commands::unsubscribe,
        runtime_tauri::commands::pause_subscription,
        runtime_tauri::commands::list_subscriptions,
        runtime_tauri::commands::sync_subscriptions,
        // resource (Phase 4)
        runtime_tauri::commands::list_resources,
        runtime_tauri::commands::search_resources,
        runtime_tauri::commands::curate_resource,
        runtime_tauri::commands::deactivate_resource,
        // sync_status (Phase 5)
        runtime_tauri::commands::get_sync_status,
    ])
    ```
  </action>
  <verify>
    <automated>cargo check -p runtime_tauri</automated>
  </verify>
  <done>
    - mod.rs 包含 `pub mod topk;` / `pub mod signal;` / `pub mod subscription;` / `pub mod resource;` / `pub mod sync_status;`
    - mod.rs 包含对应的 `pub use` 语句
    - lib.rs handler 列表包含全部 28 个命令
    - `cargo check -p runtime_tauri` 编译通过，0 errors
  </done>
</task>

</tasks>

## Verification

```bash
# 模块声明完整性
grep -c "pub mod" crates/runtime_tauri/src/commands/mod.rs  # 期望 >= 7

# handler 注册完整性
grep -c "runtime_tauri::commands::" apps/desktop-ui/src-tauri/src/lib.rs  # 期望 >= 28

# 编译验证
cargo check -p runtime_tauri
```

## Success Criteria

- [x] commands/mod.rs 包含所有 7 个模块声明
- [x] lib.rs handler 注册全部 28 个命令
- [x] cargo check 通过

## Output

After completion, create SUMMARY: `.planning/phases/05-polish-release/05-06-SUMMARY.md`
