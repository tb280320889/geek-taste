---
phase: 05-polish-release
plan: 08
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/desktop-ui/src/routes/+page.svelte
  - apps/desktop-ui/src/app.css
autonomous: true
gap_closure: true
requirements: []

must_haves:
  truths:
    - "过期数据有明确的 STALE 标识"
  artifacts:
    - path: "apps/desktop-ui/src/routes/+page.svelte"
      provides: "STALE 圆点渲染 — 导入 lastSyncedAt 和 isStale，在 Today 标题旁显示过期标识"
      min_lines: 85
    - path: "apps/desktop-ui/src/app.css"
      provides: ".stale-dot 样式"
      contains: ".stale-dot"
  key_links:
    - from: "+page.svelte"
      to: "network.ts"
      via: "import { lastSyncedAt, isStale }"
      pattern: "import.*lastSyncedAt.*from.*network"
---

# Gap Plan 08: STALE Indicator UI

**Purpose:** 将已实现的 lastSyncedAt store 和 isStale() 函数接入 +page.svelte 渲染 STALE 标识。Verification 发现 isStale() 存在但未在 UI 中调用；+page.svelte 未导入 lastSyncedAt。

**Output:** Home 页面在数据过期时（topk > 1h 或 signals > 30min）在 Today 标题旁显示琥珀色圆点。

## Context

@apps/desktop-ui/src/routes/+page.svelte
@apps/desktop-ui/src/lib/stores/network.ts
@apps/desktop-ui/src/app.css

### Current State

`+page.svelte` 84 行：
- 导入: currentUser, subscriptions, subscriptionsLoading, loadSubscriptions (无 lastSyncedAt/isStale)
- 渲染: 欢迎卡片 + 空状态引导 / 三栏卡片链接

`network.ts`:
- `lastSyncedAt` — writable<Record<string, string | null>>({})
- `isStale(source)` — 检查时间差是否超过阈值 (topk: 1h, signals: 30min)

<tasks>

<task type="auto">
  <name>Task 1: 接入 STALE 标识并添加样式</name>
  <files>apps/desktop-ui/src/routes/+page.svelte, apps/desktop-ui/src/app.css</files>
  <read_first>
    - `apps/desktop-ui/src/routes/+page.svelte` — 当前无 lastSyncedAt 导入
    - `apps/desktop-ui/src/lib/stores/network.ts` — lastSyncedAt + isStale 签名和阈值定义
    - `apps/desktop-ui/src/app.css` — 现有 CSS 变量
  </read_first>
  <action>
    1. 编辑 `apps/desktop-ui/src/routes/+page.svelte`:

    在 script 部分添加导入（在 subscriptions import 之后）：
    ```typescript
    import { lastSyncedAt, isStale } from "$lib/stores/network";
    ```

    在 isEmpty derived 之后添加 reactive 变量：
    ```typescript
    const topkStale = $derived(isStale("topk"));
    ```

    修改 h1 标签，在用户名后添加 STALE 圆点：
    ```svelte
    <h1 class="m-0 text-2xl font-semibold">
      欢迎回来，{$currentUser?.name || $currentUser?.login || "Geek"}
      {#if topkStale}
        <span class="stale-dot" title="数据可能已过期"></span>
      {/if}
    </h1>
    ```

    2. 编辑 `apps/desktop-ui/src/app.css`，在 `.offline-banner`（由 Plan 07 添加）之后追加：
    ```css
    .stale-dot {
      display: inline-block;
      width: 8px;
      height: 8px;
      border-radius: 50%;
      background: #ffc107;
      margin-left: 6px;
      vertical-align: middle;
      position: relative;
      top: -2px;
    }
    ```

    注意：此任务应在 Plan 07 之后执行，确保 .offline-banner 已存在于 app.css 中。
  </action>
  <verify>
    <automated>npx svelte-check --fail-on-warnings 2>&1 | tail -5</automated>
  </verify>
  <done>
    - +page.svelte 导入 lastSyncedAt + isStale
    - topkStale reactive 变量使用 $derived(isStale("topk"))
    - STALE 圆点在 h1 中通过 {#if topkStale} 渲染
    - app.css 包含 .stale-dot 样式（琥珀色 8px 圆点）
    - svelte-check 0 errors
  </done>
</task>

</tasks>

## Verification

```bash
# 导入检查
grep "lastSyncedAt\|isStale" apps/desktop-ui/src/routes/+page.svelte

# 渲染检查
grep "stale-dot" apps/desktop-ui/src/routes/+page.svelte
grep "topkStale" apps/desktop-ui/src/routes/+page.svelte

# 样式检查
grep ".stale-dot" apps/desktop-ui/src/app.css

# 编译检查
npx svelte-check 2>&1 | grep -c "Error"
```

## Success Criteria

- [x] lastSyncedAt 和 isStale 从 network.ts 导入
- [x] topkStale reactive 变量使用 $derived(isStale("topk"))
- [x] STALE 圆点在标题旁可见（当 topkStale 为 true 时）
- [x] svelte-check 0 errors

## Output

After completion, create SUMMARY: `.planning/phases/05-polish-release/05-08-SUMMARY.md`
