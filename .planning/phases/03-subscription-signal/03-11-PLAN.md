---
phase: 03-subscription-signal
plan: 11
type: execute
wave: 1
depends_on: ["05", "06"]
files_modified:
  - crates/application/src/subscription.rs
  - crates/persistence_sqlite/src/signal_repository.rs
  - crates/notification_adapter/src/lib.rs
  - crates/runtime_tauri/src/commands/subscription.rs
autonomous: true
requirements: [SUB-04, SUB-05, SUB-06]
gap_closure: true

must_haves:
  truths:
    - "同步时执行 U1-U4 冲突消解：RELEASE > TAG > DIGEST（per D-08）"
    - "同步后生成 12h/24h digest 聚合信号（per D-10）"
    - "发现 HIGH 信号且 notify_high_immediately=1 时触发桌面通知（per D-13）"
    - "安静时段内跳过通知但保留信号生成（per D-14）"
  artifacts:
    - path: "crates/application/src/subscription.rs"
      provides: "同步编排 + U1-U4 规则 + digest 生成 + quiet-hours 判定"
      contains: "new_digest"
    - path: "crates/notification_adapter/src/lib.rs"
      provides: "tauri-plugin-notification 薄封装（仅发送，不做业务）"
      contains: "tauri_plugin_notification"
    - path: "crates/runtime_tauri/src/commands/subscription.rs"
      provides: "sync_subscriptions 后 HIGH 通知发送链路"
      contains: "notification_adapter"
  key_links:
    - from: "application::subscription::sync_subscriptions"
      to: "signal_repository::insert_signal"
      via: "U1-U4 冲突消解后写入"
      pattern: "INSERT OR IGNORE"
    - from: "runtime_tauri::commands::sync_subscriptions"
      to: "notification_adapter"
      via: "HIGH priority immediate notify"
      pattern: "sync_subscriptions"
---

<objective>
补齐“同步规则+digest+通知”后端闭环，关闭 SUB-04/SUB-05/SUB-06 三个阻塞缺口。

Purpose: 让订阅主循环从“仅抓 release/tag”升级为“可去重、可聚合、可通知”的完整信号引擎。
Output: U1-U4 编排、digest 产出、HIGH 通知链路和 quiet-hours 约束全部落地。
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-subscription-signal/03-CONTEXT.md
@.planning/phases/03-subscription-signal/03-VERIFICATION.md
@crates/application/src/subscription.rs
@crates/persistence_sqlite/src/signal_repository.rs
@crates/runtime_tauri/src/commands/subscription.rs
@crates/runtime_tauri/src/commands/settings.rs
@crates/notification_adapter/src/lib.rs

<interfaces>
From crates/domain/src/signal.rs:
```rs
pub fn new_release(repo_id: i64, release_id: &str, title: String) -> Signal;
pub fn new_tag(repo_id: i64, tag_name: &str, title: String) -> Signal;
pub fn new_digest(repo_id: i64, bucket_start: &str, bucket_end: &str, title: String) -> Signal;
```

From crates/runtime_tauri/src/commands/settings.rs:
```rs
pub async fn get_settings(app: AppHandle) -> Result<SettingsDto, String>
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 在 sync_subscriptions 实现 U1-U4 冲突消解与 digest 聚合</name>
  <files>crates/application/src/subscription.rs, crates/persistence_sqlite/src/signal_repository.rs</files>
  <read_first>crates/application/src/subscription.rs, crates/persistence_sqlite/src/signal_repository.rs, crates/domain/src/signal.rs, .planning/phases/03-subscription-signal/03-CONTEXT.md</read_first>
  <behavior>
    - Test 1: 同 repo 同窗口若有 release，则 tag/digest 不生成
    - Test 2: 无 release 但有 tag 时，仅保留 tag（不落 digest）
    - Test 3: 仅在无 release/tag 时生成 digest，且 digest signal_key 幂等
  </behavior>
  <action>
    按 D-10 与验证缺口实现：
    1) 在 `sync_subscriptions` 内引入“本次同步候选事件”聚合，先收集 release/tag，再执行 U1-U4 决策后统一写库。
    2) digest 窗口遵循订阅 `digest_window`：`12h` 与 `24h` 两种 bucket；bucket 边界写入 `Signal::new_digest(repo_id, bucket_start, bucket_end, title)`。
    3) 若同 repo 同 bucket 内已确定 release/tag，则跳过 digest 生成；release 优先级高于 tag。
    4) 保持 `INSERT OR IGNORE` 幂等语义，不新增重复信号。
  </action>
  <verify>
    <automated>rtk cargo test -p application subscription -- --nocapture</automated>
  </verify>
  <acceptance_criteria>
    - `subscription.rs` 出现 `new_digest(` 调用
    - 同文件出现 U1-U4 判定逻辑注释或函数（包含 RELEASE/TAG/DIGEST 三分支）
    - `signal_repository.rs` 仍保留 `INSERT OR IGNORE INTO signals`
  </acceptance_criteria>
  <done>同步主循环已按 U1-U4 产出信号并具备 digest 聚合能力。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 建立 HIGH 即时通知链路并落实 quiet-hours 规则</name>
  <files>crates/notification_adapter/src/lib.rs, crates/application/src/subscription.rs, crates/runtime_tauri/src/commands/subscription.rs</files>
  <read_first>crates/notification_adapter/src/lib.rs, crates/runtime_tauri/src/commands/subscription.rs, crates/runtime_tauri/src/commands/settings.rs, crates/shared_contracts/src/settings_dto.rs, .planning/phases/03-subscription-signal/03-CONTEXT.md</read_first>
  <behavior>
    - Test 1: HIGH + notify_high_immediately=1 + 非 quiet-hours → 发送通知
    - Test 2: quiet-hours 内不发送通知，但信号仍写入数据库
    - Test 3: 通知文案格式为 `{repo_name}: {signal_type_text} — {title}`
  </behavior>
  <action>
    按 D-13/D-14/D-15/D-16 实现：
    1) `notification_adapter` 仅提供发送函数（例如 `send_high_signal_notification(...)`），内部封装 `tauri-plugin-notification`，不包含业务判断。
    2) 在 application 层新增 quiet-hours 判定函数（输入 settings.quiet_hours + 当前时间），作为“是否允许发送”的唯一业务规则。
    3) 在 `runtime_tauri::commands::sync_subscriptions` 中：执行同步后仅挑选本次新增 HIGH 信号，调用 application 判定，通过后再调用 notification_adapter 发送。
    4) 通知标题与正文严格采用 `repo_name: signal_type_text — title` 文案格式。
  </action>
  <verify>
    <automated>rtk cargo check -p runtime_tauri && rtk cargo check -p notification_adapter</automated>
  </verify>
  <acceptance_criteria>
    - `notification_adapter/src/lib.rs` 不再是空文件
    - `subscription.rs` 存在 quiet-hours 判定函数
    - `commands/subscription.rs` 存在 notification_adapter 调用
    - 代码中可检索到通知文案模板 `: ` 与 ` — ` 组合
  </acceptance_criteria>
  <done>HIGH 通知链路可工作且满足安静时段抑制规则。</done>
</task>

</tasks>

<verification>
- `rtk cargo test -p application subscription -- --nocapture`
- `rtk cargo check -p runtime_tauri`
- `rtk cargo check -p notification_adapter`
</verification>

<success_criteria>
- SUB-04/SUB-05/SUB-06 在代码链路上全部可验证
- notification_adapter 不再为空实现
</success_criteria>

<output>
After completion, create `.planning/phases/03-subscription-signal/03-11-SUMMARY.md`
</output>
