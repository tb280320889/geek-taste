---
phase: 03-subscription-signal
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/persistence_sqlite/src/lib.rs
  - crates/persistence_sqlite/src/migrations.rs
  - crates/persistence_sqlite/src/subscription_repository.rs
  - crates/persistence_sqlite/src/signal_repository.rs
  - crates/persistence_sqlite/Cargo.toml
autonomous: true
requirements:
  - SUB-01
  - SUB-02
  - SUB-03
  - SUB-08

must_haves:
  truths:
    - "V002 migration 创建 subscriptions, signals, deliveries 三张表"
    - "subscriptions 表有 signal_key UNIQUE 约束用于幂等去重"
    - "subscription_repository 支持 create, list, update_state, delete, get_by_repo_id"
    - "signal_repository 支持 insert_or_ignore (幂等), list_by_priority, mark_seen, mark_acked, count_unread"
    - "WAL 模式下并发读写正常"
  artifacts:
    - path: "crates/persistence_sqlite/src/migrations.rs"
      provides: "V002 migration 添加 subscriptions/signals/deliveries 表"
      contains: "V002"
    - path: "crates/persistence_sqlite/src/subscription_repository.rs"
      provides: "Subscription CRUD 操作"
      min_lines: 50
    - path: "crates/persistence_sqlite/src/signal_repository.rs"
      provides: "Signal CRUD + 去重 + 状态管理"
      min_lines: 50
    - path: "crates/persistence_sqlite/src/lib.rs"
      provides: "导出新模块"
      contains: "mod subscription_repository"
  key_links:
    - from: "subscription_repository.rs"
      to: "migrations.rs"
      via: "V002 subscriptions 表结构"
    - from: "signal_repository.rs"
      to: "migrations.rs"
      via: "V002 signals 表结构 + signal_key UNIQUE"
---

<objective>
Phase 3 Plan 02: SQLite 持久化层 — V002 Migration + Subscription/Signal Repository

Purpose: 创建 Phase 3 所需的数据库表和 CRUD 操作
Output: persistence_sqlite crate 新增 V002 migration + subscription_repository + signal_repository 模块
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-subscription-signal/03-CONTEXT.md
@crates/persistence_sqlite/src/lib.rs
@crates/persistence_sqlite/src/migrations.rs
@crates/persistence_sqlite/src/repo_repository.rs
@crates/persistence_sqlite/src/ranking_repository.rs
@crates/domain/src/subscription.rs
@crates/domain/src/signal.rs
@docs/05_data_model_and_contracts.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: 添加 V002 Migration</name>
  <files>crates/persistence_sqlite/src/migrations.rs</files>
  <read_first>crates/persistence_sqlite/src/migrations.rs, docs/05_data_model_and_contracts.md</read_first>
  <action>
在 migrations.rs 中添加 V002 migration（遵循 V001 的 M::up() 模式），创建三张表：

1. `subscriptions` 表:
   - subscription_id TEXT PRIMARY KEY
   - repo_id INTEGER NOT NULL REFERENCES repositories(repo_id)
   - state TEXT NOT NULL DEFAULT 'ACTIVE'
   - tracking_mode TEXT NOT NULL DEFAULT 'STANDARD'
   - event_types_json TEXT NOT NULL
   - digest_window TEXT NOT NULL DEFAULT '24h'
   - notify_high_immediately INTEGER NOT NULL DEFAULT 1
   - created_at TEXT NOT NULL
   - updated_at TEXT NOT NULL
   - last_successful_sync_at TEXT
   - cursor_release_id TEXT
   - cursor_tag_name TEXT
   - cursor_branch_sha TEXT
   - UNIQUE(repo_id, state) WHERE state = 'ACTIVE' -- 防止同一 repo 重复活跃订阅

2. `signals` 表:
   - signal_id TEXT PRIMARY KEY
   - signal_key TEXT NOT NULL UNIQUE  -- 幂等去重
   - signal_type TEXT NOT NULL
   - source_kind TEXT NOT NULL
   - repo_id INTEGER REFERENCES repositories(repo_id)
   - ranking_view_id TEXT
   - resource_id TEXT
   - priority TEXT NOT NULL
   - state TEXT NOT NULL DEFAULT 'NEW'
   - title TEXT NOT NULL
   - summary TEXT
   - evidence_json TEXT NOT NULL
   - occurred_at TEXT NOT NULL
   - bucket_start_at TEXT
   - bucket_end_at TEXT
   - created_at TEXT NOT NULL

3. `deliveries` 表:
   - delivery_id TEXT PRIMARY KEY
   - signal_id TEXT NOT NULL REFERENCES signals(signal_id)
   - channel TEXT NOT NULL
   - delivery_state TEXT NOT NULL
   - scheduled_at TEXT
   - attempted_at TEXT
   - delivered_at TEXT
   - error_code TEXT

添加索引: subscriptions(repo_id), signals(repo_id, occurred_at DESC), signals(state, priority), deliveries(signal_id)。

将 V002 添加到 migrations() 返回的 Vec 中。
  </action>
  <verify>cd crates/persistence_sqlite && cargo check</verify>
  <acceptance_criteria>
    - migrations.rs 包含 V002 migration
    - V002 创建 subscriptions 表含 signal_key UNIQUE 约束
    - V002 创建 signals 表含 signal_key UNIQUE 约束
    - V002 创建 deliveries 表
    - migrations() 返回包含 V002
  </acceptance_criteria>
  <done>V002 migration 编译通过</done>
</task>

<task type="auto">
  <name>Task 2: 创建 Subscription Repository</name>
  <files>crates/persistence_sqlite/src/subscription_repository.rs, crates/persistence_sqlite/src/lib.rs</files>
  <read_first>crates/persistence_sqlite/src/repo_repository.rs, crates/persistence_sqlite/src/ranking_repository.rs, crates/domain/src/subscription.rs</read_first>
  <action>
创建 crates/persistence_sqlite/src/subscription_repository.rs，遵循现有 repo_repository.rs 的模式：

1. `create_subscription(conn, subscription: &Subscription) -> Result<()>` — INSERT INTO subscriptions
2. `list_subscriptions(conn) -> Result<Vec<(Subscription, String)>>` — JOIN repositories 获取 full_name
3. `get_subscription_by_id(conn, subscription_id) -> Result<Option<Subscription>>`
4. `get_subscription_by_repo_id(conn, repo_id) -> Result<Option<Subscription>>` — WHERE repo_id AND state='ACTIVE'
5. `update_subscription_state(conn, subscription_id, state) -> Result<()>` — UPDATE state + updated_at
6. `delete_subscription(conn, subscription_id) -> Result<()>` — DELETE
7. `update_subscription_cursors(conn, subscription_id, cursor_release_id, cursor_tag_name, cursor_branch_sha) -> Result<()>` — UPDATE cursors + last_successful_sync_at
8. `list_active_subscriptions(conn) -> Result<Vec<Subscription>>` — WHERE state='ACTIVE'

使用 rusqlite 的 params! 宏和 query_row/query_map。Subscription 字段序列化/反序列化遵循 serde。
在 lib.rs 中添加 `pub mod subscription_repository;`。
  </action>
  <verify>cd crates/persistence_sqlite && cargo check</verify>
  <acceptance_criteria>
    - subscription_repository.rs 实现所有 CRUD 函数
    - list_subscriptions 支持 JOIN repositories
    - get_subscription_by_repo_id 使用 state='ACTIVE' 条件
    - lib.rs 包含 mod subscription_repository
    - cargo check 通过
  </acceptance_criteria>
  <done>Subscription repository 编译通过</done>
</task>

<task type="auto">
  <name>Task 3: 创建 Signal Repository</name>
  <files>crates/persistence_sqlite/src/signal_repository.rs, crates/persistence_sqlite/src/lib.rs</files>
  <read_first>crates/persistence_sqlite/src/repo_repository.rs, crates/domain/src/signal.rs</read_first>
  <action>
创建 crates/persistence_sqlite/src/signal_repository.rs：

1. `insert_signal(conn, signal: &Signal) -> Result<bool>` — INSERT OR IGNORE INTO signals, 返回是否实际插入（row_changes > 0），利用 signal_key UNIQUE 实现幂等
2. `list_signals(conn, filter_state: Option<SignalState>, filter_priority: Option<SignalPriority>, limit: i64) -> Result<Vec<Signal>>` — WHERE 条件拼接 + ORDER BY priority DESC, occurred_at DESC LIMIT
3. `list_signals_by_repo(conn, repo_id, limit) -> Result<Vec<Signal>>`
4. `get_signal_by_id(conn, signal_id) -> Result<Option<Signal>>`
5. `mark_signal_seen(conn, signal_id) -> Result<()>` — UPDATE state='SEEN' WHERE state='NEW'
6. `mark_signal_acked(conn, signal_id) -> Result<()>` — UPDATE state='ACKED' WHERE state IN ('NEW','SEEN')
7. `count_unread(conn) -> Result<i64>` — SELECT COUNT(*) WHERE state='NEW'
8. `count_unread_by_priority(conn) -> Result<(i64, i64, i64)>>` — 返回 (high, medium, low) 各自未读数

在 lib.rs 中添加 `pub mod signal_repository;`。
  </action>
  <verify>cd crates/persistence_sqlite && cargo check</verify>
  <acceptance_criteria>
    - signal_repository.rs 使用 INSERT OR IGNORE 实现幂等
    - insert_signal 返回 bool 表示是否实际插入
    - list_signals 支持 state/priority 过滤
    - count_unread 可用
    - lib.rs 包含 mod signal_repository
    - cargo check 通过
  </acceptance_criteria>
  <done>Signal repository 编译通过</done>
</task>

</tasks>

<verification>
- [ ] cd crates/persistence_sqlite && cargo check
- [ ] V002 migration 包含三张表定义
- [ ] subscription_repository 实现完整 CRUD
- [ ] signal_repository 实现幂等插入 + 状态管理
</verification>

<success_criteria>
- All tasks completed
- V002 migration creates all 3 tables with correct schema
- Subscription repository supports full CRUD
- Signal repository supports idempotent insert via INSERT OR IGNORE
- All repositories compile cleanly
</success_criteria>

<output>
After completion, create `.planning/phases/03-subscription-signal/03-02-SUMMARY.md`
</output>
