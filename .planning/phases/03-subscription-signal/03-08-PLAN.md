---
phase: 03-subscription-signal
plan: 08
type: execute
wave: 5
depends_on: ["07"]
files_modified:
  - apps/desktop-ui/src/routes/subscriptions/+page.svelte
  - apps/desktop-ui/src/lib/components/SubscriptionCard.svelte
  - apps/desktop-ui/src/lib/components/SubscriptionSearch.svelte
autonomous: true
requirements:
  - SUB-01
  - SUB-02

must_haves:
  truths:
    - "subscriptions 页面顶部有搜索框，可搜索 repo 并直接创建订阅"
    - "订阅列表显示所有订阅，每个卡片显示 repo 信息 + 状态 + 操作按钮"
    - "ACTIVE/Paused 状态切换可用"
    - "删除订阅可用"
    - "搜索结果点击后调用 subscribe IPC 创建订阅"
  artifacts:
    - path: "apps/desktop-ui/src/routes/subscriptions/+page.svelte"
      provides: "订阅管理页面 — 搜索 + 列表 + 操作"
      min_lines: 50
    - path: "apps/desktop-ui/src/lib/components/SubscriptionCard.svelte"
      provides: "订阅卡片组件"
      min_lines: 30
    - path: "apps/desktop-ui/src/lib/components/SubscriptionSearch.svelte"
      provides: "订阅搜索组件"
      min_lines: 20
  key_links:
    - from: "subscriptions/+page.svelte"
      to: "stores/subscriptions"
      via: "import loadSubscriptions, addSubscription, removeSubscription, togglePause"
    - from: "SubscriptionSearch.svelte"
      to: "ipc/tauri.ts"
      via: "搜索调用 search_repos + subscribe"
---

<objective>
Phase 3 Plan 08: Subscriptions Page UI

Purpose: 实现完整的订阅管理页面 — 搜索、创建、列表展示、状态操作
Output: 重写 subscriptions route + 新增 SubscriptionCard + SubscriptionSearch 组件
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@apps/desktop-ui/src/routes/subscriptions/+page.svelte
@apps/desktop-ui/src/routes/+layout.svelte
@apps/desktop-ui/src/lib/stores/subscriptions.ts
@apps/desktop-ui/src/lib/ipc/tauri.ts
@apps/desktop-ui/src/lib/components/SubscribePopover.svelte
@apps/desktop-ui/src/lib/components/RankingList.svelte
</context>

<tasks>

<task type="auto">
  <name>Task 1: 创建 SubscriptionSearch 组件</name>
  <files>apps/desktop-ui/src/lib/components/SubscriptionSearch.svelte</files>
  <read_first>apps/desktop-ui/src/lib/components/SubscribePopover.svelte, apps/desktop-ui/src/routes/topk/+page.svelte</read_first>
  <action>
创建 SubscriptionSearch.svelte：

1. 使用 Svelte 5 Runes ($state, $derived)
2. 输入框 + 搜索按钮 — 输入关键词后调用 search_repos IPC（已有）
3. 搜索结果显示为下拉列表，每项显示 full_name, stars, language
4. 点击结果触发 onSubscribe(repoId) 回调 — 父组件调用 addSubscription
5. 搜索防抖 300ms
6. 空状态显示 "输入 repo 名称搜索并订阅"

样式：复用 FilterPanel.svelte 的输入框风格 + RankingList 的列表项风格。
  </action>
  <verify>cd apps/desktop-ui && npx svelte-check 2>&1 | head -10</verify>
  <acceptance_criteria>
    - 组件使用 Svelte 5 Runes
    - 搜索结果列表可点击
    - 点击触发 onSubscribe 回调
  </acceptance_criteria>
  <done>SubscriptionSearch 组件完成</done>
</task>

<task type="auto">
  <name>Task 2: 创建 SubscriptionCard 组件</name>
  <files>apps/desktop-ui/src/lib/components/SubscriptionCard.svelte</files>
  <read_first>apps/desktop-ui/src/lib/components/RankingList.svelte, apps/desktop-ui/src/lib/components/RepoInfoModal.svelte</read_first>
<action>
创建 SubscriptionCard.svelte：

1. Props: subscription (Subscription), onTogglePause, onDelete
2. 显示内容：
   - 左侧：repo 全名 (链接到 GitHub) + description
   - 中间：language 标签 + stars 数 + 上次同步时间
   - 右侧：状态标签 + 操作按钮
3. 状态标签：ACTIVE=绿色 "活跃", PAUSED=黄色 "已暂停"
4. 操作按钮：暂停/恢复 toggle + 删除确认
5. 删除需要二次确认（点击删除 → 按钮变为"确认删除？" → 再点击执行）

样式：卡片布局，参考 SettingsGroup.svelte 风格。暗色适配。
  </action>
  <verify>cd apps/desktop-ui && npx svelte-check 2>&1 | head -10</verify>
  <acceptance_criteria>
    - SubscriptionCard 正确显示 Subscription 数据
    - 暂停/恢复 toggle 可用
    - 删除有二次确认
  </acceptance_criteria>
  <done>SubscriptionCard 组件完成</done>
</task>

<task type="auto">
  <name>Task 3: 重写 Subscriptions 页面</name>
  <files>apps/desktop-ui/src/routes/subscriptions/+page.svelte</files>
  <read_first>apps/desktop-ui/src/routes/subscriptions/+page.svelte, apps/desktop-ui/src/routes/topk/+page.svelte</read_first>
<action>
重写 subscriptions/+page.svelte（替换现有 placeholder）：

1. 使用 Svelte 5 Runes
2. import { subscriptions, loadSubscriptions, addSubscription, removeSubscription, togglePause } from '$lib/stores/subscriptions'
3. import { searchRepos } from '$lib/ipc/tauri'
4. onMount 调用 loadSubscriptions()
5. 页面布局：
   - 顶部：SubscriptionSearch 组件（搜索框）
   - 主体：订阅卡片列表 — 遍历 $subscriptions 渲染 SubscriptionCard
   - 空状态：无订阅时显示 "还没有订阅，搜索 repo 开始跟踪"
6. 搜索结果点击后调用 addSubscription(repoId) + toast 通知

样式：与 topk 页面保持一致的 page 布局（padding, max-width）。
  </action>
  <verify>cd apps/desktop-ui && npx svelte-check 2>&1 | head -10</verify>
  <acceptance_criteria>
    - 页面不再是 placeholder 文本
    - 搜索框 + 订阅列表 + 空状态全部可用
    - 创建/暂停/删除订阅流程可操作
  </acceptance_criteria>
  <done>Subscriptions 页面重写完成</done>
</task>

</tasks>

<verification>
- [ ] cd apps/desktop-ui && npx svelte-check
- [ ] Subscriptions 页面可渲染无错误
- [ ] 订阅 CRUD 操作流程可用
</verification>

<success_criteria>
- All tasks completed
- Subscriptions page fully functional with search, create, list, pause, delete
- UI style consistent with existing pages
</success_criteria>

<output>
After completion, create `.planning/phases/03-subscription-signal/03-08-SUMMARY.md`
</output>
