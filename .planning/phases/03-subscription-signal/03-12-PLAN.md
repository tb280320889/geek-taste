---
phase: 03-subscription-signal
plan: 12
type: execute
wave: 2
depends_on: ["11"]
files_modified:
  - crates/persistence_sqlite/src/signal_repository.rs
  - crates/application/src/signal.rs
  - crates/runtime_tauri/src/commands/signal.rs
  - apps/desktop-ui/src/lib/stores/signals.ts
autonomous: true
requirements: [HOME-01, HOME-02]
gap_closure: true

must_haves:
  truths:
    - "Home 仅展示自上次访问以来的新高优先级摘要（per D-09）"
    - "Home 排序包含 priority + time + source_kind + user affinity 维度"
    - "last_visit 游标在前端持久化并参与后端查询"
  artifacts:
    - path: "apps/desktop-ui/src/lib/stores/signals.ts"
      provides: "last_visit 读写与 listHomeSignals 调用参数"
      contains: "last_visit"
    - path: "crates/application/src/signal.rs"
      provides: "home 查询参数接收与 DTO 输出"
      contains: "since"
    - path: "crates/persistence_sqlite/src/signal_repository.rs"
      provides: "带 since + source_kind + affinity 的排序查询"
      contains: "ORDER BY"
  key_links:
    - from: "signals.ts"
      to: "runtime_tauri::list_home_signals"
      via: "since timestamp"
      pattern: "listHomeSignals"
    - from: "application::signal"
      to: "signal_repository::list_home_signals"
      via: "query args mapping"
      pattern: "list_home_signals"
---

<objective>
补齐 Home 信号流“上次访问增量 + 多因子排序”能力，关闭 HOME-01/HOME-02 缺口。

Purpose: 让 Home 真正体现“高信噪比摘要”，而非简单按时间倒序列表。
Output: since-last-visit 游标机制与后端排序 contract 一致落地。
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-subscription-signal/03-CONTEXT.md
@.planning/phases/03-subscription-signal/03-VERIFICATION.md
@crates/persistence_sqlite/src/signal_repository.rs
@crates/application/src/signal.rs
@crates/runtime_tauri/src/commands/signal.rs
@apps/desktop-ui/src/lib/stores/signals.ts
@apps/desktop-ui/src/routes/+page.svelte

<interfaces>
From apps/desktop-ui/src/lib/ipc/tauri.ts (to update):
```ts
export const listHomeSignals = async (...): Promise<SignalDto[]>
```

From crates/runtime_tauri/src/commands/signal.rs (to update):
```rs
pub async fn list_home_signals(app: AppHandle, since: Option<String>) -> Result<Vec<SignalDto>, String>
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 扩展 Home 查询 contract（since + 排序因子）</name>
  <files>crates/persistence_sqlite/src/signal_repository.rs, crates/application/src/signal.rs, crates/runtime_tauri/src/commands/signal.rs</files>
  <read_first>crates/persistence_sqlite/src/signal_repository.rs, crates/application/src/signal.rs, crates/runtime_tauri/src/commands/signal.rs, docs/04_ranking_subscription_notification_spec.md</read_first>
  <behavior>
    - Test 1: 传入 since 后，仅返回 occurred_at > since 的信号
    - Test 2: 排序优先级为 priority DESC → occurred_at DESC → source_kind weight DESC → affinity weight DESC
    - Test 3: 不传 since 时保持兼容，返回旧行为超集
  </behavior>
  <action>
    1) 修改 signal repository：`list_home_signals(conn, since: Option<&str>, limit: i64, language_interests: &[String])`。
    2) SQL 增加排序维度：
       - priority 权重：HIGH=3, MEDIUM=2, LOW=1
       - source_kind 权重：REPOSITORY=3, RANKING_VIEW=2, RESOURCE=1
       - user_affinity：先按 repo primary_language 命中 `language_interests` 给 1，否则 0
       - 最后 occurred_at DESC 保证新鲜度
    3) application 与 runtime_tauri 同步扩展函数签名，透传 since 与（可用时）语言兴趣参数。
  </action>
  <verify>
    <automated>rtk cargo test -p persistence_sqlite signal_repository -- --nocapture && rtk cargo check -p runtime_tauri</automated>
  </verify>
  <acceptance_criteria>
    - `signal_repository.rs` 的 `list_home_signals` 签名含 `since` 参数
    - SQL 含 `source_kind` 与 affinity 权重排序表达式
    - `runtime_tauri::list_home_signals` 接收可选 `since`
  </acceptance_criteria>
  <done>后端支持 since-last-visit 与四维排序。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 前端落地 last_visit 游标并改造 Home 加载流程</name>
  <files>apps/desktop-ui/src/lib/stores/signals.ts</files>
  <read_first>apps/desktop-ui/src/lib/stores/signals.ts, apps/desktop-ui/src/routes/+page.svelte, apps/desktop-ui/src/lib/ipc/tauri.ts</read_first>
  <behavior>
    - Test 1: 首次加载无游标时可正常取数
    - Test 2: 二次加载时会携带上次写入的 last_visit
    - Test 3: load 成功后写回新的 last_visit
  </behavior>
  <action>
    1) 在 signals store 增加 `last_visit` 持久化键（localStorage，键名 `home_signals_last_visit`）。
    2) `loadHomeSignals()` 调用 IPC 时附带 `since=last_visit`，成功后更新 last_visit 为当前 ISO 时间。
    3) 若调用失败，不覆盖旧 last_visit；保证失败可重试。
  </action>
  <verify>
    <automated>rtk pnpm exec svelte-check --tsconfig ./apps/desktop-ui/tsconfig.json</automated>
  </verify>
  <acceptance_criteria>
    - `signals.ts` 出现常量 `home_signals_last_visit`
    - `loadHomeSignals` 调用 listHomeSignals 时传入 since
    - 成功分支有 last_visit 更新逻辑
  </acceptance_criteria>
  <done>Home 信号流已具备“自上次访问以来”的增量语义。</done>
</task>

</tasks>

<verification>
- `rtk cargo test -p persistence_sqlite signal_repository -- --nocapture`
- `rtk cargo check -p runtime_tauri`
- `rtk pnpm exec svelte-check --tsconfig ./apps/desktop-ui/tsconfig.json`
</verification>

<success_criteria>
- HOME-01/HOME-02 的核心缺口关闭（last_visit + 多因子排序）
- Home 页面加载逻辑与后端 contract 一致
</success_criteria>

<output>
After completion, create `.planning/phases/03-subscription-signal/03-12-SUMMARY.md`
</output>
