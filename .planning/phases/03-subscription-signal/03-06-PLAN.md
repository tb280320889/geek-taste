---
phase: 03-subscription-signal
plan: 06
type: execute
wave: 3
depends_on: ["05"]
files_modified:
  - crates/runtime_tauri/Cargo.toml
  - crates/runtime_tauri/src/commands/mod.rs
  - crates/runtime_tauri/src/commands/subscription.rs
  - crates/runtime_tauri/src/commands/signal.rs
  - apps/desktop-ui/src-tauri/src/lib.rs
autonomous: true
requirements:
  - SUB-01
  - SUB-02
  - SUB-06
  - SUB-07
  - HOME-01

must_haves:
  truths:
    - "subscribe / unsubscribe / pause_subscription / list_subscriptions 四个 Tauri command 可用"
    - "sync_subscriptions command 触发全量同步"
    - "list_signals / ack_signal / mark_signal_seen / get_unread_counts 四个 Tauri command 可用"
    - "所有 command 遵循 async fn -> Result<T, String> 模式"
    - "所有 command 注册到 lib.rs generate_handler![]"
  artifacts:
    - path: "crates/runtime_tauri/src/commands/subscription.rs"
      provides: "订阅相关 Tauri IPC 命令"
      min_lines: 30
    - path: "crates/runtime_tauri/src/commands/signal.rs"
      provides: "信号相关 Tauri IPC 命令"
      min_lines: 30
    - path: "apps/desktop-ui/src-tauri/src/lib.rs"
      provides: "注册新命令到 generate_handler"
      contains: "subscribe"
  key_links:
    - from: "commands/subscription.rs"
      to: "application::subscription"
      via: "薄封装调用 application 层"
    - from: "commands/signal.rs"
      to: "application::signal"
      via: "薄封装调用 application 层"
    - from: "lib.rs"
      to: "commands/subscription.rs + commands/signal.rs"
      via: "generate_handler! 注册"
---

<objective>
Phase 3 Plan 06: Tauri IPC Commands

Purpose: 将 application 层的订阅和信号功能暴露为前端可调用的 IPC 命令
Output: runtime_tauri crate 新增 subscription + signal 命令模块，注册到 Tauri handler
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@crates/runtime_tauri/src/commands/mod.rs
@crates/runtime_tauri/src/commands/topk.rs
@crates/runtime_tauri/src/commands/auth.rs
@crates/runtime_tauri/src/lib.rs
@apps/desktop-ui/src-tauri/src/lib.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: 创建 Subscription IPC Commands</name>
  <files>crates/runtime_tauri/src/commands/subscription.rs, crates/runtime_tauri/src/commands/mod.rs</files>
  <read_first>crates/runtime_tauri/src/commands/topk.rs, crates/runtime_tauri/src/commands/auth.rs</read_first>
<action>
创建 crates/runtime_tauri/src/commands/subscription.rs，遵循 topk.rs 的薄封装模式：

1. `#[tauri::command] pub async fn subscribe(app: AppHandle, repo_id: i64, tracking_mode: Option<String>, event_types: Option<Vec<String>>, digest_window: Option<String>, notify_high_immediately: Option<bool>) -> Result<SubscriptionDto, String>`:
   - get_db_connection(&app)?
   - 构建 CreateSubscriptionRequest
   - 调用 application::subscription::subscribe
   - .map_err(|e| e.to_string())

2. `#[tauri::command] pub async fn unsubscribe(app: AppHandle, subscription_id: String) -> Result<(), String>`
3. `#[tauri::command] pub async fn pause_subscription(app: AppHandle, subscription_id: String) -> Result<(), String>`
4. `#[tauri::command] pub async fn list_subscriptions(app: AppHandle) -> Result<Vec<SubscriptionDto>, String>`
5. `#[tauri::command] pub async fn sync_subscriptions(app: AppHandle) -> Result<usize, String>`

在 mod.rs 中添加 `pub mod subscription;` 和 `pub use subscription::*;`。

在 apps/desktop-ui/src-tauri/src/lib.rs 的 generate_handler![] 中添加这 5 个命令。
  </action>
  <verify>cd crates/runtime_tauri && cargo check</verify>
  <acceptance_criteria>
    - subscription.rs 包含 5 个 Tauri command
    - mod.rs 导出 subscription 模块
    - lib.rs generate_handler 包含 subscribe, unsubscribe, pause_subscription, list_subscriptions, sync_subscriptions
    - cargo check 通过
  </acceptance_criteria>
  <done>Subscription IPC commands 编译通过</done>
</task>

<task type="auto">
  <name>Task 2: 创建 Signal IPC Commands</name>
  <files>crates/runtime_tauri/src/commands/signal.rs, crates/runtime_tauri/src/commands/mod.rs</files>
  <read_first>crates/runtime_tauri/src/commands/subscription.rs</read_first>
<action>
创建 crates/runtime_tauri/src/commands/signal.rs：

1. `#[tauri::command] pub async fn list_signals(app: AppHandle, filter_state: Option<String>, limit: Option<i64>) -> Result<Vec<SignalDto>, String>`
2. `#[tauri::command] pub async fn list_home_signals(app: AppHandle) -> Result<Vec<SignalDto>, String>`
3. `#[tauri::command] pub async fn ack_signal(app: AppHandle, signal_id: String) -> Result<(), String>`
4. `#[tauri::command] pub async fn mark_signal_seen(app: AppHandle, signal_id: String) -> Result<(), String>`
5. `#[tauri::command] pub async fn get_unread_counts(app: AppHandle) -> Result<UnreadCountsDto, String>`

在 mod.rs 中添加 `pub mod signal;` 和 `pub use signal::*;`。

在 apps/desktop-ui/src-tauri/src/lib.rs 的 generate_handler![] 中添加这 5 个命令。
  </action>
  <verify>cd crates/runtime_tauri && cargo check</verify>
  <acceptance_criteria>
    - signal.rs 包含 5 个 Tauri command
    - mod.rs 导出 signal 模块
    - lib.rs generate_handler 包含所有 signal 命令
    - cargo check 通过
  </acceptance_criteria>
  <done>Signal IPC commands 编译通过</done>
</task>

</tasks>

<verification>
- [ ] cd crates/runtime_tauri && cargo check
- [ ] cd apps/desktop-ui && cargo check (完整 Tauri app)
- [ ] 所有 10 个新命令在 generate_handler! 中注册
</verification>

<success_criteria>
- All tasks completed
- 10 new Tauri commands registered and accessible from frontend
- All commands follow established thin-wrapper pattern
</success_criteria>

<output>
After completion, create `.planning/phases/03-subscription-signal/03-06-SUMMARY.md`
</output>
