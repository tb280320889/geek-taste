---
phase: 03-subscription-signal
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/domain/src/lib.rs
  - crates/domain/src/subscription.rs
  - crates/domain/src/signal.rs
autonomous: true
requirements:
  - SUB-01
  - SUB-02
  - SUB-03
  - SUB-04
  - SUB-07
  - SUB-08

must_haves:
  truths:
    - "Subscription 领域对象包含 id, repo_id, state, tracking_mode, event_types, digest_window, cursors 等字段"
    - "Signal 领域对象包含 signal_id, signal_key, signal_type, priority, state, evidence 等字段"
    - "SubscriptionState 枚举支持 ACTIVE/PAUSED/ARCHIVED 状态转换"
    - "SignalType 枚举包含 RELEASE_PUBLISHED, TAG_PUBLISHED, DEFAULT_BRANCH_ACTIVITY_DIGEST 等"
    - "SignalState 枚举支持 NEW -> SEEN -> ACKED 状态机"
    - "signal_key 生成逻辑符合规范: repo_id:TYPE:unique_id"
  artifacts:
    - path: "crates/domain/src/subscription.rs"
      provides: "Subscription 领域对象 + SubscriptionState/TrackingMode 枚举"
      contains: "enum SubscriptionState"
    - path: "crates/domain/src/signal.rs"
      provides: "Signal 领域对象 + SignalType/SignalState/SignalPriority 枚举"
      contains: "enum SignalType"
    - path: "crates/domain/src/lib.rs"
      provides: "导出 subscription 和 signal 模块"
      contains: "mod subscription"
  key_links:
    - from: "crates/domain/src/signal.rs"
      to: "crates/domain/src/subscription.rs"
      via: "Signal.source_kind 引用 Subscription 的 repo_id"
---

<objective>
Phase 3 Plan 01: 领域模型层 — Subscription + Signal

Purpose: 建立 Phase 3 所需的纯领域对象，后续所有 crate 依赖此层
Output: domain crate 新增 subscription + signal 模块
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-subscription-signal/03-CONTEXT.md
@crates/domain/src/lib.rs
@crates/domain/src/repository.rs
@crates/domain/src/ranking.rs
@docs/05_data_model_and_contracts.md
@docs/04_ranking_subscription_notification_spec.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: 创建 Subscription 领域对象</name>
  <files>crates/domain/src/subscription.rs, crates/domain/src/lib.rs</files>
  <read_first>crates/domain/src/repository.rs, crates/domain/src/ranking.rs, docs/05_data_model_and_contracts.md</read_first>
  <action>
在 crates/domain/src/subscription.rs 中创建：

1. `SubscriptionState` 枚举: Active, Paused, Archived（用 serde rename 全大写）
2. `TrackingMode` 枚举: Standard, Advanced
3. `Subscription` struct:
   - subscription_id: String (ULID)
   - repo_id: i64 (FK -> repositories)
   - state: SubscriptionState
   - tracking_mode: TrackingMode
   - event_types: Vec<String> (存储为 JSON)
   - digest_window: String ("12h" | "24h")
   - notify_high_immediately: bool
   - created_at: String (ISO8601)
   - updated_at: String (ISO8601)
   - last_successful_sync_at: Option<String>
   - cursor_release_id: Option<String>
   - cursor_tag_name: Option<String>
   - cursor_branch_sha: Option<String>
4. 实现 Default trait — 默认 state=Active, tracking_mode=Standard, digest_window="24h", notify_high_immediately=true
5. 实现 `Subscription::new(repo_id)` 构造函数 — 生成 ULID subscription_id, 设置默认 event_types 为 ["RELEASE_PUBLISHED", "TAG_PUBLISHED", "DEFAULT_BRANCH_ACTIVITY_DIGEST"]

遵循现有 domain 模块风格（参考 repository.rs）：pub struct + derive(Debug, Clone, Serialize, Deserialize) + thiserror 错误类型。

在 lib.rs 中添加 `pub mod subscription;`。
  </action>
  <verify>cd crates/domain && cargo check</verify>
  <acceptance_criteria>
    - subscription.rs 包含 "enum SubscriptionState"
    - subscription.rs 包含 "enum TrackingMode"
    - subscription.rs 包含 "struct Subscription"
    - lib.rs 包含 "mod subscription"
    - cargo check 通过无错误
  </acceptance_criteria>
  <done>Subscription 领域对象可编译通过</done>
</task>

<task type="auto">
  <name>Task 2: 创建 Signal 领域对象</name>
  <files>crates/domain/src/signal.rs, crates/domain/src/lib.rs</files>
  <read_first>crates/domain/src/subscription.rs (刚创建), docs/05_data_model_and_contracts.md, docs/04_ranking_subscription_notification_spec.md</read_first>
  <action>
在 crates/domain/src/signal.rs 中创建：

1. `SignalType` 枚举: ReleasePublished, ReleasePrereleased, TagPublished, DefaultBranchActivityDigest, PrMergedDigest, TopkViewChanged, ResourceEmerged, ResourceReranked（serde rename ALL_CAPS 如 "RELEASE_PUBLISHED"）
2. `SignalPriority` 枚举: High, Medium, Low
3. `SignalState` 枚举: New, Seen, Acked, Archived
4. `SourceKind` 枚举: Repository, RankingView, Resource
5. `Signal` struct:
   - signal_id: String (ULID)
   - signal_key: String (UNIQUE, 用于幂等去重)
   - signal_type: SignalType
   - source_kind: SourceKind
   - repo_id: Option<i64>
   - ranking_view_id: Option<String>
   - resource_id: Option<String>
   - priority: SignalPriority
   - state: SignalState
   - title: String
   - summary: Option<String>
   - evidence: serde_json::Value
   - occurred_at: String (ISO8601)
   - bucket_start_at: Option<String>
   - bucket_end_at: Option<String>
   - created_at: String (ISO8601)
6. 实现 `Signal::new_release(repo_id, release_id, title)` 辅助函数 — 自动生成 signal_key = "{repo_id}:RELEASE_PUBLISHED:{release_id}"
7. 实现 `Signal::new_tag(repo_id, tag_name, title)` — signal_key = "{repo_id}:TAG_PUBLISHED:{tag_name}"
8. 实现 `Signal::new_digest(repo_id, bucket_start, bucket_end, title)` — signal_key = "{repo_id}:DEFAULT_BRANCH_ACTIVITY_DIGEST:{bucket_start}:{bucket_end}"

在 lib.rs 中添加 `pub mod signal;`。
  </action>
  <verify>cd crates/domain && cargo check</verify>
  <acceptance_criteria>
    - signal.rs 包含 "enum SignalType"
    - signal.rs 包含 "enum SignalPriority"
    - signal.rs 包含 "enum SignalState"
    - signal.rs 包含 "struct Signal"
    - signal.rs 包含 "fn new_release"
    - signal_key 生成格式符合 docs/05 去重键规则
    - lib.rs 包含 "mod signal"
    - cargo check 通过
  </acceptance_criteria>
  <done>Signal 领域对象可编译通过</done>
</task>

</tasks>

<verification>
- [ ] cd crates/domain && cargo check
- [ ] cd crates/domain && cargo test (如有)
- [ ] lib.rs 导出 subscription 和 signal 模块
</verification>

<success_criteria>
- All tasks completed
- Domain crate compiles with new modules
- Subscription and Signal structs match data model spec (docs/05)
- signal_key generation follows dedup key rules
</success_criteria>

<output>
After completion, create `.planning/phases/03-subscription-signal/03-01-SUMMARY.md`
</output>
