---
phase: 03-subscription-signal
plan: 04
type: execute
wave: 2
depends_on: ["01"]
files_modified:
  - crates/shared_contracts/src/lib.rs
  - crates/shared_contracts/src/subscription_dto.rs
  - crates/shared_contracts/src/signal_dto.rs
autonomous: true
requirements:
  - SUB-01
  - SUB-02
  - SUB-07
  - HOME-01
  - HOME-02

must_haves:
  truths:
    - "SubscriptionDto 包含订阅展示所需的所有字段 + repo full_name"
    - "SignalDto / SignalCard 包含信号卡片展示所需字段"
    - "DTO 与领域对象之间有 From trait 双向转换"
    - "CreateSubscriptionRequest 用于前端创建订阅传参"
  artifacts:
    - path: "crates/shared_contracts/src/subscription_dto.rs"
      provides: "SubscriptionDto, CreateSubscriptionRequest, UpdateSubscriptionRequest"
    - path: "crates/shared_contracts/src/signal_dto.rs"
      provides: "SignalDto (SignalCard), SignalTypeDto, SignalPriorityDto"
    - path: "crates/shared_contracts/src/lib.rs"
      provides: "导出新 DTO 模块"
      contains: "mod subscription_dto"
  key_links:
    - from: "subscription_dto.rs"
      to: "crates/domain/src/subscription.rs"
      via: "From<Subscription> trait 实现"
    - from: "signal_dto.rs"
      to: "crates/domain/src/signal.rs"
      via: "From<Signal> trait 实现"
---

<objective>
Phase 3 Plan 04: Shared Contracts — Subscription + Signal DTO

Purpose: 定义前后端数据传输契约，遵循现有 DTO 模式
Output: shared_contracts crate 新增 subscription_dto + signal_dto 模块
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@crates/shared_contracts/src/lib.rs
@crates/shared_contracts/src/ranking_dto.rs
@crates/shared_contracts/src/auth_dto.rs
@crates/domain/src/subscription.rs
@crates/domain/src/signal.rs
@docs/05_data_model_and_contracts.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: 创建 Subscription DTO</name>
  <files>crates/shared_contracts/src/subscription_dto.rs, crates/shared_contracts/src/lib.rs</files>
  <read_first>crates/shared_contracts/src/ranking_dto.rs, crates/domain/src/subscription.rs</read_first>
  <action>
创建 crates/shared_contracts/src/subscription_dto.rs：

1. `SubscriptionDto` struct (derive Serialize, Deserialize, Clone):
   - subscription_id: String
   - repo_id: i64
   - full_name: String (从 JOIN repositories 获取)
   - html_url: String
   - description: Option<String>
   - primary_language: Option<String>
   - stargazers_count: i64
   - state: String ("ACTIVE"/"PAUSED"/"ARCHIVED")
   - tracking_mode: String
   - event_types: Vec<String>
   - digest_window: String
   - notify_high_immediately: bool
   - last_successful_sync_at: Option<String>
   - created_at: String

2. `CreateSubscriptionRequest` struct:
   - repo_id: i64
   - tracking_mode: Option<String> (默认 "STANDARD")
   - event_types: Option<Vec<String>> (默认 ["RELEASE_PUBLISHED", "TAG_PUBLISHED", "DEFAULT_BRANCH_ACTIVITY_DIGEST"])
   - digest_window: Option<String> (默认 "24h")
   - notify_high_immediately: Option<bool> (默认 true)

3. `UpdateSubscriptionRequest` struct:
   - state: Option<String> (用于暂停/恢复)
   - event_types: Option<Vec<String>>
   - digest_window: Option<String>
   - notify_high_immediately: Option<bool>

4. 实现 `From<domain::subscription::Subscription> for SubscriptionDto`（注意：full_name/html_url 等 repo 字段在 From 转换时不填充，由 repository 层 JOIN 后手动设置）

遵循 ranking_dto.rs 的 derive 和命名模式。

在 lib.rs 中添加 `pub mod subscription_dto;`。
  </action>
  <verify>cd crates/shared_contracts && cargo check</verify>
  <acceptance_criteria>
    - subscription_dto.rs 包含 SubscriptionDto, CreateSubscriptionRequest, UpdateSubscriptionRequest
    - From<Subscription> 实现存在
    - lib.rs 包含 mod subscription_dto
  </acceptance_criteria>
  <done>Subscription DTO 编译通过</done>
</task>

<task type="auto">
  <name>Task 2: 创建 Signal DTO</name>
  <files>crates/shared_contracts/src/signal_dto.rs, crates/shared_contracts/src/lib.rs</files>
  <read_first>crates/shared_contracts/src/ranking_dto.rs, crates/domain/src/signal.rs, docs/05_data_model_and_contracts.md</read_first>
  <action>
创建 crates/shared_contracts/src/signal_dto.rs：

1. `SignalDto` struct (对应 SignalCard contract):
   - signal_id: String
   - signal_type: String
   - priority: String ("HIGH"/"MEDIUM"/"LOW")
   - state: String ("NEW"/"SEEN"/"ACKED"/"ARCHIVED")
   - source_kind: String
   - repo_id: Option<i64>
   - full_name: Option<String> (从 JOIN 获取)
   - title: String
   - summary: Option<String>
   - evidence: serde_json::Value
   - occurred_at: String
   - created_at: String

2. `UnreadCountsDto` struct:
   - total: i64
   - high: i64
   - medium: i64
   - low: i64

3. 实现 `From<domain::signal::Signal> for SignalDto`

在 lib.rs 中添加 `pub mod signal_dto;`。
  </action>
  <verify>cd crates/shared_contracts && cargo check</verify>
  <acceptance_criteria>
    - signal_dto.rs 包含 SignalDto (SignalCard) 和 UnreadCountsDto
    - From<Signal> 实现存在
    - lib.rs 包含 mod signal_dto
  </acceptance_criteria>
  <done>Signal DTO 编译通过</done>
</task>

</tasks>

<verification>
- [ ] cd crates/shared_contracts && cargo check
- [ ] DTO 字段匹配 docs/05 Contract 定义
</verification>

<success_criteria>
- All tasks completed
- DTOs match the contract definitions in docs/05
- From trait implementations bridge domain and transport layers
</success_criteria>

<output>
After completion, create `.planning/phases/03-subscription-signal/03-04-SUMMARY.md`
</output>
