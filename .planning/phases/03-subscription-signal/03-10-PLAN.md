---
phase: 03-subscription-signal
plan: 10
type: execute
wave: 1
depends_on: ["08"]
files_modified:
  - apps/desktop-ui/src/lib/components/SubscriptionSearch.svelte
  - apps/desktop-ui/src/routes/subscriptions/+page.svelte
  - apps/desktop-ui/src/lib/stores/subscriptions.ts
autonomous: true
requirements: [SUB-01]
gap_closure: true

must_haves:
  truths:
    - "用户在 /subscriptions 页面顶部搜索仓库后可直接创建订阅（per D-01）"
    - "搜索结果点击后触发订阅创建并在列表即时可见（per D-02）"
    - "订阅创建失败时页面显示明确错误，不吞错"
  artifacts:
    - path: "apps/desktop-ui/src/lib/components/SubscriptionSearch.svelte"
      provides: "可订阅的搜索结果列表与 onSubscribe 回调"
      contains: "onSubscribe"
    - path: "apps/desktop-ui/src/routes/subscriptions/+page.svelte"
      provides: "搜索结果到 addSubscription 的接线"
      contains: "addSubscription"
    - path: "apps/desktop-ui/src/lib/stores/subscriptions.ts"
      provides: "创建后去重更新/重载逻辑"
      contains: "addSubscription"
  key_links:
    - from: "SubscriptionSearch.svelte"
      to: "subscriptions/+page.svelte"
      via: "onSubscribe(repoId)"
      pattern: "onSubscribe"
    - from: "subscriptions/+page.svelte"
      to: "stores/subscriptions.ts"
      via: "addSubscription(repoId)"
      pattern: "addSubscription"
---

<objective>
修复订阅页“可搜索但不可创建订阅”的断链，闭合 SUB-01 用户路径。

Purpose: 补齐验证报告的首个 blocker，确保订阅入口集中在 /subscriptions（per D-01）。
Output: 搜索组件支持订阅回调，页面完成创建接线与错误反馈。
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-subscription-signal/03-CONTEXT.md
@.planning/phases/03-subscription-signal/03-VERIFICATION.md
@apps/desktop-ui/src/lib/components/SubscriptionSearch.svelte
@apps/desktop-ui/src/routes/subscriptions/+page.svelte
@apps/desktop-ui/src/lib/stores/subscriptions.ts

<interfaces>
From apps/desktop-ui/src/lib/stores/subscriptions.ts:
```ts
export async function addSubscription(repoId: number, options?: CreateSubscriptionRequest)
```

From apps/desktop-ui/src/lib/ipc/tauri.ts:
```ts
export const fetchRepoInfo = async (owner: string, repo: string): Promise<RepoBasicInfo>
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 为 SubscriptionSearch 增加“可订阅结果”回调</name>
  <files>apps/desktop-ui/src/lib/components/SubscriptionSearch.svelte</files>
  <read_first>apps/desktop-ui/src/lib/components/SubscriptionSearch.svelte, apps/desktop-ui/src/lib/components/SubscribePopover.svelte, apps/desktop-ui/src/lib/types.ts</read_first>
  <behavior>
    - Test 1: 输入 owner/repo 并命中结果时，组件渲染“订阅”按钮并可点击
    - Test 2: 点击“订阅”按钮时，向父组件触发 onSubscribe(repo_id)
    - Test 3: owner/repo 格式非法时不触发查询
  </behavior>
  <action>
    按 D-01/D-02 修改组件：
    1) 在 props 中新增必需回调 `onSubscribe: (repoId: number) => void | Promise<void>`。
    2) 保留现有 `fetchRepoInfo(owner, repo)` 查询，但在结果卡新增“订阅此仓库”按钮；按钮点击触发 `onSubscribe(result.repo_id)`，并在请求中展示 loading/disabled。
    3) 删除“在 TopK 中发现后可订阅”的误导文案（验证报告已标记错位）。
    4) 查询失败时继续显示 `searchError`，不吞错；owner/repo 正则校验维持前置拦截。
  </action>
  <verify>
    <automated>rtk pnpm exec svelte-check --tsconfig ./apps/desktop-ui/tsconfig.json</automated>
  </verify>
  <acceptance_criteria>
    - `SubscriptionSearch.svelte` 包含 `onSubscribe` prop 定义
    - 文件中出现“订阅此仓库”按钮且按钮 click 调用 `onSubscribe(`
    - 文件不再包含文案“在 TopK 中发现此项目后可直接订阅”
  </acceptance_criteria>
  <done>搜索组件已具备可执行订阅动作，不再是只读预览。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 在订阅页面接线 addSubscription 并处理去重/错误</name>
  <files>apps/desktop-ui/src/routes/subscriptions/+page.svelte, apps/desktop-ui/src/lib/stores/subscriptions.ts</files>
  <read_first>apps/desktop-ui/src/routes/subscriptions/+page.svelte, apps/desktop-ui/src/lib/stores/subscriptions.ts, apps/desktop-ui/src/lib/ipc/tauri.ts</read_first>
  <behavior>
    - Test 1: 子组件回调 repo_id 后，页面调用 addSubscription(repoId)
    - Test 2: 重复订阅同 repo 时不会在 subscriptions store 产生重复项
    - Test 3: 订阅失败时页面可见错误提示
  </behavior>
  <action>
    1) 在 `subscriptions/+page.svelte` 引入 `addSubscription`，将其作为 `SubscriptionSearch` 的 `onSubscribe` 传入，并在回调里执行创建后刷新列表状态。
    2) 在 `stores/subscriptions.ts` 的 `addSubscription` 中加入 repo_id 级去重更新（若已存在则替换对应项或直接返回现有值），避免 UI 重复卡片。
    3) 创建失败时把错误透传到 `subscriptionsError`，在页面错误区可见。
    4) 保持“手动同步”按钮和现有 pause/delete 行为不回归。
  </action>
  <verify>
    <automated>rtk pnpm exec svelte-check --tsconfig ./apps/desktop-ui/tsconfig.json</automated>
  </verify>
  <acceptance_criteria>
    - `subscriptions/+page.svelte` 含 `addSubscription` import 与 `onSubscribe` 传参
    - `stores/subscriptions.ts` 的 `addSubscription` 存在 repo_id 去重逻辑
    - 失败分支会写入 `subscriptionsError`
  </acceptance_criteria>
  <done>/subscriptions 已成为“搜索即订阅”的可用入口，满足 SUB-01。</done>
</task>

</tasks>

<verification>
- 运行 `rtk pnpm exec svelte-check --tsconfig ./apps/desktop-ui/tsconfig.json` 必须通过（允许既有 warning）
- 手动 smoke：在 /subscriptions 输入 `owner/repo` 后可直接订阅并看到新卡片
</verification>

<success_criteria>
- 验证报告中“SubscriptionSearch 未接线”缺口关闭
- 用户无需跳转 TopK 即可在订阅页创建订阅
</success_criteria>

<output>
After completion, create `.planning/phases/03-subscription-signal/03-10-SUMMARY.md`
</output>
