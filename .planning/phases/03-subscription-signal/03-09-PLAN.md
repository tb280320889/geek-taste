---
phase: 03-subscription-signal
plan: 09
type: execute
wave: 5
depends_on: ["07"]
files_modified:
  - apps/desktop-ui/src/routes/+page.svelte
  - apps/desktop-ui/src/lib/components/SignalCard.svelte
  - apps/desktop-ui/src/lib/components/SubscribePopover.svelte
  - apps/desktop-ui/src/routes/topk/+page.svelte
autonomous: true
requirements:
  - SUB-05
  - SUB-06
  - SUB-07
  - HOME-01
  - HOME-02

must_haves:
  truths:
    - "SignalCard 组件显示信号优先级色条 + 类型图标 + title + summary + 时间"
    - "Home 页面显示信号 feed — 按 priority + time 排序"
    - "信号卡片点击后自动标记 SEEN"
    - "提供「标记已处理」按钮设为 ACKED"
    - "Home 页面空状态显示引导文案"
    - "SubscribePopover 的 onConfirm 现在调用 subscribe IPC"
    - "TopK RankingList 的 is_subscribed 从 subscribedRepoIds store 获取"
  artifacts:
    - path: "apps/desktop-ui/src/lib/components/SignalCard.svelte"
      provides: "信号卡片组件 — 优先级色条 + 内容 + 操作"
      min_lines: 30
    - path: "apps/desktop-ui/src/routes/+page.svelte"
      provides: "Home 页面 — 信号摘要 + 引导"
      contains: "signal"
    - path: "apps/desktop-ui/src/lib/components/SubscribePopover.svelte"
      provides: "修复 onConfirm 接入实际 subscribe IPC"
      contains: "subscribe"
  key_links:
    - from: "+page.svelte (home)"
      to: "stores/signals"
      via: "loadHomeSignals on mount"
    - from: "SignalCard.svelte"
      to: "stores/signals"
      via: "markSeenAction + ackSignalAction"
    - from: "SubscribePopover.svelte"
      to: "stores/subscriptions"
      via: "addSubscription 接入实际 IPC"
    - from: "RankingList.svelte"
      to: "stores/subscriptions"
      via: "subscribedRepoIds derived store"
---

<objective>
Phase 3 Plan 09: Signal UI + Home Page + SubscribePopover 接入

Purpose: 实现信号卡片、Home 页面信号流、接入 SubscribePopover 和 TopK 订阅状态
Output: 新增 SignalCard 组件，重写 Home 页面，修复 SubscribePopover + RankingList 接入
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@apps/desktop-ui/src/routes/+page.svelte
@apps/desktop-ui/src/lib/stores/signals.ts
@apps/desktop-ui/src/lib/stores/subscriptions.ts
@apps/desktop-ui/src/lib/components/SubscribePopover.svelte
@apps/desktop-ui/src/lib/components/RankingList.svelte
@apps/desktop-ui/src/routes/topk/+page.svelte
</context>

<tasks>

<task type="auto">
  <name>Task 1: 创建 SignalCard 组件</name>
  <files>apps/desktop-ui/src/lib/components/SignalCard.svelte</files>
  <read_first>apps/desktop-ui/src/lib/components/RankingList.svelte, apps/desktop-ui/src/lib/types.ts</read_first>
<action>
创建 SignalCard.svelte：

1. Props: signal (Signal), onAck (callback), onSeen (callback)
2. 布局：
   - 左侧细竖条：HIGH=#ef4444 红, MEDIUM=#f59e0b 黄, LOW=#6b7280 灰
   - 主体：signal_type 图标 + title + summary
   - 右侧：occurred_at 相对时间（如 "2h ago"）+ 操作按钮
3. 状态样式：
   - NEW: 加粗 title + 背景微高亮
   - SEEN: 正常字重
   - ACKED: 灰色 + 删除线（可选）
4. 操作：
   - 点击卡片主体 → markSeen（如果 state=NEW）
   - 「标记已处理」按钮 → ackSignal
5. 信号类型图标（Unicode 文字替代，不引入 icon 库）：
   - RELEASE_PUBLISHED: "[Release]"
   - TAG_PUBLISHED: "[Tag]"
   - DEFAULT_BRANCH_ACTIVITY_DIGEST: "[Commit]"

样式：卡片圆角 + 暗色背景 + hover 微亮。参考 Linear 通知列表。
  </action>
  <verify>cd apps/desktop-ui && npx svelte-check 2>&1 | head -10</verify>
  <acceptance_criteria>
    - SignalCard 正确显示优先级色条
    - 点击触发 markSeen
    - ACK 按钮可用
    - 三种状态视觉区分清晰
  </acceptance_criteria>
  <done>SignalCard 组件完成</done>
</task>

<task type="auto">
  <name>Task 2: 重写 Home 页面 — 信号摘要</name>
  <files>apps/desktop-ui/src/routes/+page.svelte</files>
  <read_first>apps/desktop-ui/src/routes/+page.svelte, apps/desktop-ui/src/lib/stores/signals.ts</read_first>
<action>
重写 +page.svelte（替换现有 link cards 布局）：

1. 使用 Svelte 5 Runes
2. import { homeSignals, unreadCounts, loadHomeSignals, ackSignalAction, markSeenAction } from '$lib/stores/signals'
3. onMount 调用 loadHomeSignals()
4. 页面布局：
   - 顶部：标题 "Today" + 未读计数 badge
   - 主体：信号 feed — 遍历 $homeSignals 渲染 SignalCard
   - HIGH 优先级信号在顶部区域突出显示
   - 空状态：「暂无新信号」+ 引导按钮「去 TopK 发现」和「管理订阅」
5. 保留原有的快速入口卡片（缩小），放在信号列表下方

样式：参考 Linear 的 Today view — 日期分组 + 信号列表。
  </action>
  <verify>cd apps/desktop-ui && npx svelte-check 2>&1 | head -10</verify>
  <acceptance_criteria>
    - Home 页面显示信号 feed
    - 未读计数 badge 显示正确
    - 空状态引导可用
    - 信号卡片交互（markSeen + ack）工作
  </acceptance_criteria>
  <done>Home 页面信号摘要完成</done>
</task>

<task type="auto">
  <name>Task 3: 修复 SubscribePopover + RankingList 接入</name>
  <files>apps/desktop-ui/src/lib/components/SubscribePopover.svelte, apps/desktop-ui/src/routes/topk/+page.svelte, apps/desktop-ui/src/lib/components/RankingList.svelte</files>
  <read_first>apps/desktop-ui/src/lib/components/SubscribePopover.svelte, apps/desktop-ui/src/routes/topk/+page.svelte, apps/desktop-ui/src/lib/stores/subscriptions.ts</read_first>
<action>
修改 3 个文件：

1. SubscribePopover.svelte:
   - 修改 onConfirm 回调签名，添加 repoId 参数
   - 内部调用 addSubscription(repoId) 而非仅关闭 popover
   - 成功后 toast "订阅成功"

2. topk/+page.svelte:
   - import { addSubscription } from '$lib/stores/subscriptions'
   - handleSubscribeConfirm 调用 addSubscription(repoId) + 关闭 popover

3. RankingList.svelte:
   - import { subscribedRepoIds } from '$lib/stores/subscriptions'
   - 将 RankingItem 的 is_subscribed 替换为 $subscribedRepoIds.has(item.repoId)
   - 移除硬编码的 is_subscribed: false
  </action>
  <verify>cd apps/desktop-ui && npx svelte-check 2>&1 | head -10</verify>
  <acceptance_criteria>
    - SubscribePopover onConfirm 调用实际 subscribe IPC
    - RankingList 的 is_subscribed 从 store 动态获取
    - TopK 页面订阅后按钮变为「已订阅」
  </acceptance_criteria>
  <done>SubscribePopover + RankingList 接入完成</done>
</task>

</tasks>

<verification>
- [ ] cd apps/desktop-ui && npx svelte-check
- [ ] Home 页面渲染信号 feed
- [ ] TopK 订阅按钮状态正确更新
- [ ] SignalCard 交互正常
</verification>

<success_criteria>
- All tasks completed
- Signal card component displays priority, type, title with proper state styling
- Home page shows signal feed sorted by priority + time
- SubscribePopover actually creates subscriptions via IPC
- TopK RankingList shows correct is_subscribed from store
</success_criteria>

<output>
After completion, create `.planning/phases/03-subscription-signal/03-09-SUMMARY.md`
</output>
